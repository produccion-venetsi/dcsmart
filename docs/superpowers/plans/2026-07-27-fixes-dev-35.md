# Fixes DEV-35 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar los 7 fixes pedidos el 2026-07-27 más el arreglo del suite de tests del backend, cada uno como un commit independiente en `DEV-35`.

**Architecture:** Los 8 ítems son independientes: ninguno consume código de otro, y se pueden revisar y revertir por separado. La lógica de negocio nueva que se puede aislar (regla del estado CAJA, parseo de "OP-101", armado de columnas del export) se extrae a módulos puros bajo `lib/` con tests `node --test`; el resto es cableado de UI que se verifica a mano en la app corriendo. El único ítem que toca la base de producción (observaciones en Arqueo) va último y su migración tiene un gate de aprobación humana.

**Tech Stack:** Node 22, Fastify 5, Prisma (PostgreSQL en Cloud SQL), React 19 + Vite 8, Zustand, SheetJS (`xlsx` 0.18, edición community), `node:test` + `node:assert/strict`.

**Spec:** `docs/superpowers/specs/2026-07-27-fixes-dev-35-design.md`

## Global Constraints

- Branch: `DEV-35`. **No pushear ni mergear a `dev` ni a `master`** sin permiso explícito del usuario en el momento — `dev` y `master` despliegan al mismo backend de Cloud Run, así que un merge a `dev` llega a producción.
- Un commit por tarea. Mensajes en español, en imperativo, con prefijo `feat:` / `fix:` / `chore:` / `docs:`.
- Convenciones del proyecto (CLAUDE.md): ESModules, `async/await`, campos de base en `snake_case`, montos `Decimal(12,2)`.
- **Los números de línea de este plan son del estado inicial del repo.** Cada tarea que modifica un archivo lo corre unas líneas. Ubicá los cambios buscando el fragmento de código citado, no la línea.
- El frontend **no tiene runner de tests**: solo se testean módulos puros de `frontend/src/lib/` (JS sin JSX) con `node --test`. Los componentes React se verifican a mano.
- Verificación manual: requiere los 3 servicios locales arriba — `cloud-sql-proxy` en 5433, backend en 3000, frontend en 5173.
- No tocar `docs/superpowers/plans/2026-07-14-taptap-sync.md` ni `responsive-bugs/` (untracked a propósito).

---

## Orden y dependencias

Las tareas van de menor a mayor riesgo. **Tarea 1 primero** (habilita los tests para el resto). **Tarea 8 última** (necesita aprobación humana para la migración). Entre medio el orden es libre, salvo que **la Tarea 6 depende de la Tarea 3** (ambas tocan `PAGO_CSV_COLUMNS`; hacerlas al revés genera un conflicto tonto).

| Tarea | Ítem del spec | Toca backend | Toca base | Tests |
|---|---|---|---|---|
| 1 | 8 — glob de tests | package.json | no | sí (habilita) |
| 2 | 5 — buscador RubCat | no | no | manual |
| 3 | 2 — columna observaciones | no | no | manual |
| 4 | 1 — filtro observaciones | sí | no | manual |
| 5 | 4 — Actividad | sí | no | sí |
| 6 | 7 — exportación | sí | no | sí |
| 7 | 3 — estado CAJA | sí | no | sí |
| 8 | 6 — observaciones Arqueo | sí | **sí** | manual + gate |

---

### Task 1: Arreglar el glob de tests del backend

El script apunta a `src/test/**/*.test.js`, carpeta que no existe. Los dos archivos de test reales viven en `src/jobs/taptap/` y quedan fuera, así que `npm test` reporta éxito habiendo corrido 0 tests. Node 22 descubre `*.test.js` recursivamente sin glob y excluye `node_modules` por su cuenta.

**Files:**
- Modify: `backend/package.json` (línea `"test": "node --test src/test/**/*.test.js"`)

**Interfaces:**
- Consumes: nada.
- Produces: `npm test` en `backend/` corre todos los `*.test.js` del proyecto. Las tareas 5, 6 y 7 dependen de esto.

- [ ] **Step 1: Confirmar el problema**

```bash
cd backend && npm test
```

Esperado: termina en `# tests 0` con exit code 0 — verde habiendo corrido nada.

- [ ] **Step 2: Cambiar el script**

En `backend/package.json`, reemplazar:

```json
    "test": "node --test src/test/**/*.test.js",
```

por:

```json
    "test": "node --test",
```

- [ ] **Step 3: Verificar que ahora sí corre**

```bash
cd backend && npm test
```

Esperado: `# tests 8`, `# pass 8`, `# fail 0`. Los 8 vienen de `src/jobs/taptap/mapping.test.js` y `src/jobs/taptap/metodos.test.js`.

- [ ] **Step 4: Commit**

```bash
git add backend/package.json
git commit -m "fix(tests): el glob del script de test dejaba 8 tests sin correr

El script apuntaba a src/test/**/*.test.js, una carpeta que no existe,
asi que npm test terminaba en verde con 0 tests ejecutados. Node 22
descubre los *.test.js recursivamente sin glob explicito."
```

---

### Task 2: Buscador en las tres pestañas de Rubro / Categoría / RubCat

`RubCat.jsx` carga las tres listas completas en memoria (`load()`), así que el filtrado es en cliente y no toca el backend. `NombreSection` es el componente compartido por Rubros y Categorías: el input se escribe una vez ahí y sirve para las dos pestañas.

**Files:**
- Modify: `frontend/src/pages/admin/RubCat.jsx` (componente `NombreSection` en `:30`; bloque `tab === 'rubcat'` en `:236-274`)

**Interfaces:**
- Consumes: nada.
- Produces: nada que otras tareas usen.

- [ ] **Step 1: Agregar el buscador a `NombreSection` (cubre Rubros y Categorías)**

En `NombreSection`, sumar el estado del filtro junto a los que ya existen:

```jsx
  const [form,    setForm]    = useState('')
  const [editing, setEditing] = useState(null)
  const [saving,  setSaving]  = useState(false)
  const [filtro,  setFiltro]  = useState('')
```

Y derivar la lista visible antes del `return`:

```jsx
  const q = filtro.trim().toLowerCase()
  const visibles = q
    ? items.filter(i => (i.nombre || '').toLowerCase().includes(q))
    : items
```

- [ ] **Step 2: Renderizar el input y usar la lista filtrada**

Reemplazar el encabezado de la tabla de `NombreSection`:

```jsx
        <div style={{ padding: '0.75rem 1.25rem', borderBottom: '1px solid var(--border)', fontWeight: 700, fontSize: 13 }}>
          {title}s <span style={{ color: 'var(--t3)', fontWeight: 400 }}>({items.length})</span>
        </div>
```

por una versión con buscador y contador que distingue filtrado de total:

```jsx
        <div style={{ padding: '0.75rem 1.25rem', borderBottom: '1px solid var(--border)', display: 'flex', alignItems: 'center', gap: '0.75rem' }}>
          <span style={{ fontWeight: 700, fontSize: 13, whiteSpace: 'nowrap' }}>
            {title}s{' '}
            <span style={{ color: 'var(--t3)', fontWeight: 400 }}>
              ({q ? `${visibles.length} de ${items.length}` : items.length})
            </span>
          </span>
          <div className="form-input-wrap" style={{ marginLeft: 'auto', maxWidth: 220 }}>
            <input
              placeholder={`Buscar ${title.toLowerCase()}...`}
              value={filtro}
              onChange={e => setFiltro(e.target.value)}
            />
          </div>
        </div>
```

Y en el `<tbody>` del mismo componente, cambiar las dos referencias a `items` por `visibles`:

```jsx
            {visibles.map(item => (
```

```jsx
            {visibles.length === 0 && (
              <tr><td colSpan={2} style={{ textAlign: 'center', padding: '2rem', color: 'var(--t3)' }}>
                {q ? 'Sin resultados para la búsqueda' : 'Sin datos'}
              </td></tr>
            )}
```

- [ ] **Step 3: Agregar el buscador a la pestaña RubCat**

En el componente `RubCat`, junto a los otros `useState`:

```jsx
  const [rcFiltro, setRcFiltro] = useState('')
```

Y antes del `return`, la lista filtrada por los seis campos:

```jsx
  const rcQ = rcFiltro.trim().toLowerCase()
  const rubcatVisible = rcQ
    ? rubcat.filter(rc => [
        rc.rubro?.nombre, rc.categoria?.nombre, rc.cuenta,
        rc.tipo, rc.costo, rc.clasificacion,
      ].some(v => (v || '').toLowerCase().includes(rcQ)))
    : rubcat
```

- [ ] **Step 4: Renderizar el input de RubCat y usar la lista filtrada**

Reemplazar el encabezado del bloque `tab === 'rubcat'`:

```jsx
              <div style={{ padding: '0.75rem 1.25rem', borderBottom: '1px solid var(--border)', fontWeight: 700, fontSize: 13 }}>
                RubCat <span style={{ color: 'var(--t3)', fontWeight: 400 }}>({rubcat.length})</span>
              </div>
```

por:

```jsx
              <div style={{ padding: '0.75rem 1.25rem', borderBottom: '1px solid var(--border)', display: 'flex', alignItems: 'center', gap: '0.75rem' }}>
                <span style={{ fontWeight: 700, fontSize: 13, whiteSpace: 'nowrap' }}>
                  RubCat{' '}
                  <span style={{ color: 'var(--t3)', fontWeight: 400 }}>
                    ({rcQ ? `${rubcatVisible.length} de ${rubcat.length}` : rubcat.length})
                  </span>
                </span>
                <div className="form-input-wrap" style={{ marginLeft: 'auto', maxWidth: 260 }}>
                  <input
                    placeholder="Buscar rubro, categoría, cuenta, tipo..."
                    value={rcFiltro}
                    onChange={e => setRcFiltro(e.target.value)}
                  />
                </div>
              </div>
```

En el `<tbody>`, cambiar `rubcat.map((rc) =>` por `rubcatVisible.map((rc) =>`, y el estado vacío:

```jsx
                  {rubcatVisible.length === 0 && (
                    <tr><td colSpan={7} style={{ textAlign: 'center', padding: '2rem', color: 'var(--t3)' }}>
                      {rcQ ? 'Sin resultados para la búsqueda' : 'Sin combinaciones'}
                    </td></tr>
                  )}
```

- [ ] **Step 5: Verificar en la app**

Con los 3 servicios arriba, entrar a la pantalla de Rubros/Categorías/RubCat (menú de administración) y comprobar:

1. Pestaña **RubCat**: escribir parte de un rubro → la tabla filtra mientras se tipea; el contador muestra `(N de M)`.
2. Escribir el nombre de una **categoría** en el mismo input → también filtra (busca en los 6 campos).
3. Escribir algo inexistente (`zzz`) → dice "Sin resultados para la búsqueda", no "Sin combinaciones".
4. Borrar el input → vuelve la lista completa y el contador vuelve a un solo número.
5. Pestañas **Rubros** y **Categorías**: mismo comportamiento.
6. Con un filtro activo, hacer clic en una fila → sigue abriendo la edición correctamente.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/admin/RubCat.jsx
git commit -m "feat(rubcat): buscador en las pestanas de RubCat, Rubros y Categorias

Filtra en memoria (las listas ya se cargan completas). En RubCat busca
en rubro, categoria, cuenta, tipo, costo y clasificacion a la vez. El
contador pasa a (N de M) con filtro activo para que no parezca que se
borraron registros."
```

---

### Task 3: Columna Observaciones en la tabla de pagos

Columna nueva entre **Método** y **Cashflow**, truncada, con el texto completo en el `title` nativo (el patrón que ya usan las columnas Aud, E/I y Foto).

**Files:**
- Modify: `frontend/src/pages/pagos/PagoList.jsx` (`colCount` en `:1206`; `<th>` tras `:1519`; `<td>` tras `:1582`)

**Interfaces:**
- Consumes: `p.observaciones` — ya viene en la respuesta de `GET /pagos`, no hace falta tocar el backend.
- Produces: nada. **La Tarea 6 debe hacerse después de esta** (ambas tocan `PAGO_CSV_COLUMNS`).

- [ ] **Step 1: Actualizar `colCount`**

Está hardcodeado y lo usan el skeleton de carga y el `colSpan` del estado vacío. Reemplazar:

```jsx
  const colCount = 18 + (showLocalCol ? 1 : 0) + (selectionMode ? 1 : 0)
```

por:

```jsx
  const colCount = 19 + (showLocalCol ? 1 : 0) + (selectionMode ? 1 : 0)
```

- [ ] **Step 2: Agregar el `<th>`**

En el `<thead>`, después de `<th>Método</th>` y antes de `<th>Cashflow</th>`:

```jsx
              <th>Método</th>
              <th style={{ minWidth: 140 }}>Observaciones</th>
              <th>Cashflow</th>
```

- [ ] **Step 3: Agregar el `<td>`**

En el `<tbody>`, después del `<td>` del método y antes del de cashflow:

```jsx
                  <td style={{ minWidth: 120, fontSize: 12 }}>{p.metodo_pago?.nombre || <span className="td-muted">—</span>}</td>
                  <td
                    style={{ maxWidth: 200, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap', fontSize: 12 }}
                    title={p.observaciones || ''}
                  >
                    {p.observaciones || <span className="td-muted">—</span>}
                  </td>
                  <td style={{ minWidth: 90 }}>{fmtDate(p.cashflow)}</td>
```

- [ ] **Step 4: Verificar en la app**

En la pantalla de Pagos:

1. La columna **Observaciones** aparece entre Método y Cashflow.
2. Un pago con observación larga se muestra cortado con `…` y **no** ensancha la fila ni descuadra la tabla.
3. Pasar el mouse por encima → aparece el tooltip del sistema con el texto completo (tarda ~1s, es lo normal del `title` nativo).
4. Un pago sin observaciones muestra `—` y al pasar el mouse no aparece tooltip vacío.
5. Recargar con la tabla en estado de carga → las columnas del skeleton coinciden con las de la tabla (esto valida `colCount`).
6. Filtrar por algo que no exista para forzar la tabla vacía → el mensaje "No hay pagos…" ocupa el ancho completo, sin columna sobrante a la derecha.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/pagos/PagoList.jsx
git commit -m "feat(pagos): columna Observaciones despues de Metodo

Truncada con ellipsis y el texto completo en el title nativo, el mismo
patron de las columnas Aud/E-I/Foto. Se actualiza colCount, que estaba
hardcodeado en 18 y lo usan el skeleton y el colSpan de la tabla vacia."
```

---

### Task 4: Filtro dedicado por observaciones en pagos

Filtro de texto propio, separado del buscador general, para que buscar por observaciones no traiga además coincidencias por nombre de proveedor. Va en el `where` compartido, así que aplica a la tabla, al resumen y al export sin duplicar lógica.

**Files:**
- Modify: `backend/src/routes/pagos.js` (`buildPagosWhere` en `:146-151` y `:194-220`; destructuring y llamada de `GET /` en `:234-250`; ídem `GET /summary` en `:297-311`)
- Modify: `frontend/src/pages/pagos/PagoList.jsx` (`FILTER_INIT` en `:818`; `buildParams` en `:894-907`; panel de filtros en `:1285-1350`)

**Interfaces:**
- Consumes: nada.
- Produces: parámetro de query `observaciones` (string) aceptado por `GET /pagos` y `GET /pagos/summary`.

- [ ] **Step 1: Aceptar el parámetro en `buildPagosWhere`**

En el destructuring de la función (`pagos.js:147-151`), agregar `observaciones` al final:

```js
  const {
    id_local, id_proveedor, id_proveedores, pagado, estado_op,
    desde, hasta, campo_fecha, id_tipo, id_rub, id_cat, id_rubcat, id_rubcats,
    audit, ingresa_egreso, id_metodo, nro_ord, cmv_quick, q, observaciones
  } = query
```

Y en el objeto que devuelve, junto a los otros filtros opcionales (después de la línea de `estado_op`):

```js
    ...(estado_op      ? { estado_op }                                    : {}),
    ...(observaciones?.trim()
      ? { observaciones: { contains: observaciones.trim(), mode: 'insensitive' } }
      : {}),
```

- [ ] **Step 2: Pasar el parámetro desde los dos llamadores**

Hay exactamente **2** llamadores. En `GET /` (`:234`) agregar `observaciones` al destructuring de `request.query` y al objeto que se le pasa:

```js
    const {
      id_local, id_proveedor, id_proveedores, pagado, estado_op,
      desde, hasta, campo_fecha, id_tipo, id_rub, id_cat, id_rubcat, id_rubcats,
      audit, ingresa_egreso, id_metodo, nro_ord, cmv_quick, q, observaciones,
      sort_field = 'fecha', sort_dir = 'desc',
      page = 1, limit = 50
    } = request.query
```

```js
    const where = await buildPagosWhere(fastify, request, {
      id_local, id_proveedor, id_proveedores, pagado, estado_op,
      desde, hasta, campo_fecha, id_tipo, id_rub, id_cat, id_rubcat, id_rubcats,
      audit, ingresa_egreso, id_metodo, nro_ord, cmv_quick, q, observaciones
    })
```

Hacer lo mismo en `GET /summary` (`:297` y `:307`), con sus dos bloques equivalentes. Si el parámetro no llega a `/summary`, el cuadro de totales deja de coincidir con la tabla.

- [ ] **Step 3: Agregar el filtro al estado del frontend**

En `PagoList.jsx`, `FILTER_INIT`:

```jsx
const FILTER_INIT = {
  pagado: '', estado_op: '', campo_fecha: 'fecha', desde: '', hasta: '',
  id_tipo: '', id_rub: '', id_cat: '',
  audit: '', ingresa_egreso: '', id_metodo: '', cmv_quick: '',
  observaciones: '',
  id_proveedores: [],
  id_rubcats: [],
}
```

`activeFilterCount` (`:1113`) recorre `filters` genéricamente, así que el contador del botón "Filtros" cuenta este filtro sin cambios extra.

- [ ] **Step 4: Enviarlo en la query**

En `buildParams`, junto a los otros:

```jsx
      ...(filters.observaciones.trim()  ? { observaciones:   filters.observaciones.trim() } : {}),
```

- [ ] **Step 5: Agregar el control al panel de filtros**

En el grid del panel, después del bloque de "Dirección" y antes del de "Tipo de fecha", a ancho completo:

```jsx
                  <div style={{ gridColumn: '1 / -1' }}>
                    <span style={lbl}>Observaciones</span>
                    <div className="form-input-wrap">
                      <input
                        placeholder="Contiene el texto..."
                        value={draft.observaciones}
                        onChange={e => setDraftField('observaciones', e.target.value)}
                      />
                    </div>
                  </div>
```

El panel usa el patrón `draft` → `applyFilters()`, así que el filtro recién se aplica al confirmar, como el resto.

- [ ] **Step 6: Verificar en la app**

1. Abrir Filtros, escribir en **Observaciones** un texto que exista en algún pago y aplicar → la tabla trae solo esos pagos.
2. Probar con **distinta capitalización** (`FLETE` vs `flete`) → mismo resultado (es case-insensitive).
3. El botón "Filtros" muestra el contador incrementado en 1.
4. Con un rango de fechas **desde+hasta** cargado además del filtro, verificar que el cuadro de TOTAL IMPORTE de arriba corresponda a las filas filtradas (esto valida el Step 2 sobre `/summary`).
5. Exportar a Excel con el filtro puesto → el archivo trae solo las filas filtradas.
6. "Limpiar filtros" → el campo queda vacío y vuelven todos los pagos.

- [ ] **Step 7: Commit**

```bash
git add backend/src/routes/pagos.js frontend/src/pages/pagos/PagoList.jsx
git commit -m "feat(pagos): filtro por observaciones

Filtro dedicado (no se suma al buscador general) para que buscar por
observaciones no traiga tambien coincidencias por proveedor. Se agrega
al where compartido, asi aplica igual a la tabla, al summary y al
export."
```

---

### Task 5: Actividad — nombres reales y buscador de OPs

Dos problemas en una pantalla: el detalle del evento muestra UUIDs crudos donde debería mostrar nombres, y no hay forma de buscar una OP puntual.

**Files:**
- Create: `backend/src/lib/nroOrd.js`
- Create: `backend/src/lib/nroOrd.test.js`
- Create: `backend/src/lib/snapshotLabels.js`
- Create: `backend/src/lib/snapshotLabels.test.js`
- Modify: `backend/src/routes/activity_log.js` (`GET /` completo, `:8-55`)
- Modify: `backend/src/routes/pagos.js` (`:179`, para usar el parser compartido)
- Modify: `frontend/src/pages/activity-log/ActivityLog.jsx` (`snapshotRows` en `:17-38`; `FILTER_INIT` en `:41`; `buildParams` en `:62-69`; panel de filtros en `:95-124`)

**Interfaces:**
- Consumes: nada.
- Produces:
  - `parseNroOrd(input: string) => number | null` — de `backend/src/lib/nroOrd.js`. Acepta `"101"`, `"OP-101"`, `"op 101"`; devuelve `null` si no hay número.
  - `etiquetarSnapshot(snapshot: object, catalogos: { proveedores: Map, rubcats: Map, metodos: Map, locales: Map }) => object` — de `backend/src/lib/snapshotLabels.js`. Devuelve `{ proveedor, rubcat, metodo, local, tipo }`, todos strings.
  - `GET /activity-log` acepta `nro_ord` (string) y devuelve cada evento con un campo nuevo `snapshot_labels`.

- [ ] **Step 1: Escribir el test del parser de OP**

Crear `backend/src/lib/nroOrd.test.js`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { parseNroOrd } from './nroOrd.js'

test('parseNroOrd acepta un numero suelto', () => {
  assert.equal(parseNroOrd('101'), 101)
})

test('parseNroOrd acepta el prefijo OP en cualquier forma', () => {
  assert.equal(parseNroOrd('OP-101'), 101)
  assert.equal(parseNroOrd('op-101'), 101)
  assert.equal(parseNroOrd('OP 101'), 101)
  assert.equal(parseNroOrd('op101'), 101)
})

test('parseNroOrd ignora espacios alrededor', () => {
  assert.equal(parseNroOrd('  OP-101  '), 101)
})

test('parseNroOrd devuelve null si no hay numero', () => {
  assert.equal(parseNroOrd(''), null)
  assert.equal(parseNroOrd('   '), null)
  assert.equal(parseNroOrd('coca cola'), null)
  assert.equal(parseNroOrd('OP-'), null)
  assert.equal(parseNroOrd(null), null)
  assert.equal(parseNroOrd(undefined), null)
})
```

- [ ] **Step 2: Correr el test y verificar que falla**

```bash
cd backend && npm test
```

Esperado: FAIL — `Cannot find module './nroOrd.js'`.

- [ ] **Step 3: Implementar el parser**

Crear `backend/src/lib/nroOrd.js`:

```js
// Parsea lo que el usuario escribe en un buscador de OPs: "101", "OP-101",
// "op 101". Devuelve null si no hay un numero adentro, para que el llamador
// distinga "no busques" de "busca el 0".
export function parseNroOrd(input) {
  if (typeof input !== 'string') return null
  const limpio = input.trim().replace(/^op[-\s]*/i, '')
  if (limpio === '') return null
  const n = parseInt(limpio, 10)
  return Number.isNaN(n) ? null : n
}
```

- [ ] **Step 4: Correr el test y verificar que pasa**

```bash
cd backend && npm test
```

Esperado: los 6 tests nuevos de `nroOrd` en verde, más los 8 que ya existían.

- [ ] **Step 5: Escribir el test del etiquetador de snapshots**

Crear `backend/src/lib/snapshotLabels.test.js`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { etiquetarSnapshot } from './snapshotLabels.js'

const catalogos = {
  proveedores: new Map([['p1', 'Coca-Cola FEMSA']]),
  rubcats:     new Map([['rc1', 'CMV Bebidas / Gaseosas']]),
  metodos:     new Map([['m1', 'Efectivo']]),
  locales:     new Map([['l1', 'Palermo']]),
}

test('etiquetarSnapshot resuelve los ids a nombres', () => {
  const labels = etiquetarSnapshot(
    { id_proveedor: 'p1', id_rubcat: 'rc1', id_metodo: 'm1', id_local: 'l1', id_tipo: 'A' },
    catalogos
  )
  assert.equal(labels.proveedor, 'Coca-Cola FEMSA')
  assert.equal(labels.rubcat,    'CMV Bebidas / Gaseosas')
  assert.equal(labels.metodo,    'Efectivo')
  assert.equal(labels.local,     'Palermo')
})

test('etiquetarSnapshot marca los ids que ya no existen', () => {
  const labels = etiquetarSnapshot({ id_proveedor: 'borrado', id_rubcat: 'rc1' }, catalogos)
  assert.equal(labels.proveedor, '— (no existe)')
})

test('etiquetarSnapshot deja em-dash si el id es null o falta', () => {
  const labels = etiquetarSnapshot({ id_rubcat: 'rc1' }, catalogos)
  assert.equal(labels.proveedor, '—')
  assert.equal(labels.metodo,    '—')
})

test('etiquetarSnapshot muestra DC_1 y DC_2 como DC (1) y DC (2)', () => {
  assert.equal(etiquetarSnapshot({ id_tipo: 'DC_1' }, catalogos).tipo, 'DC (1)')
  assert.equal(etiquetarSnapshot({ id_tipo: 'DC_2' }, catalogos).tipo, 'DC (2)')
})

test('etiquetarSnapshot deja los demas tipos tal cual', () => {
  assert.equal(etiquetarSnapshot({ id_tipo: 'A' },   catalogos).tipo, 'A')
  assert.equal(etiquetarSnapshot({ id_tipo: 'NCA' }, catalogos).tipo, 'NCA')
  assert.equal(etiquetarSnapshot({ id_tipo: null },  catalogos).tipo, '—')
})

test('etiquetarSnapshot no explota con snapshot vacio', () => {
  const labels = etiquetarSnapshot(null, catalogos)
  assert.equal(labels.proveedor, '—')
  assert.equal(labels.tipo,      '—')
})
```

- [ ] **Step 6: Correr el test y verificar que falla**

```bash
cd backend && npm test
```

Esperado: FAIL — `Cannot find module './snapshotLabels.js'`.

- [ ] **Step 7: Implementar el etiquetador**

Crear `backend/src/lib/snapshotLabels.js`:

```js
// El snapshot del ActivityLog guarda las columnas crudas del pago, con ids.
// Esto los traduce a texto legible. Un id que no resuelve significa que el
// registro se borro despues del evento: se dice explicitamente, en vez de
// mostrar un UUID.
const TIPO_LABEL = { DC_1: 'DC (1)', DC_2: 'DC (2)' }

function resolver(id, catalogo) {
  if (id == null || id === '') return '—'
  return catalogo.get(id) ?? '— (no existe)'
}

export function etiquetarSnapshot(snapshot, catalogos) {
  const s = snapshot ?? {}
  return {
    proveedor: resolver(s.id_proveedor, catalogos.proveedores),
    rubcat:    resolver(s.id_rubcat,    catalogos.rubcats),
    metodo:    resolver(s.id_metodo,    catalogos.metodos),
    local:     resolver(s.id_local,     catalogos.locales),
    tipo:      s.id_tipo == null || s.id_tipo === ''
      ? '—'
      : (TIPO_LABEL[s.id_tipo] ?? s.id_tipo),
  }
}
```

- [ ] **Step 8: Correr el test y verificar que pasa**

```bash
cd backend && npm test
```

Esperado: todo verde (8 previos + 6 de `nroOrd` + 6 de `snapshotLabels`).

- [ ] **Step 9: Usar el parser compartido en pagos.js**

En `buildPagosWhere`, reemplazar el parseo inline por el módulo, para no tener dos versiones de la misma regla. Agregar el import arriba del archivo:

```js
import { parseNroOrd } from '../lib/nroOrd.js'
```

Y en el bloque del buscador general, cambiar:

```js
    const qNum = parseInt(qStr.replace(/^op[-\s]*/i, ''))
    qFilter = {
      OR: [
        ...(!isNaN(qNum) ? [{ nro_ord: qNum }] : []),
```

por:

```js
    const qNum = parseNroOrd(qStr)
    qFilter = {
      OR: [
        ...(qNum != null ? [{ nro_ord: qNum }] : []),
```

- [ ] **Step 10: Enriquecer y filtrar en `GET /activity-log`**

En `activity_log.js`, agregar los imports:

```js
import { parseNroOrd } from '../lib/nroOrd.js'
import { etiquetarSnapshot } from '../lib/snapshotLabels.js'
```

Aceptar `nro_ord` en el destructuring de la query:

```js
    const {
      desde, hasta, tabla, id_user, accion, id_local, nro_ord,
      page = 1, limit = 50
    } = request.query
```

Validar y filtrar. El filtro va sobre la columna JSON, comparando el número guardado en `snapshot.nro_ord`:

```js
    let nroOrdFilter = {}
    if (nro_ord != null && String(nro_ord).trim() !== '') {
      const n = parseNroOrd(String(nro_ord))
      if (n == null) {
        return reply.code(400).send({ error: 'El buscador de OP espera un número (ej: 101 u OP-101)' })
      }
      nroOrdFilter = { snapshot: { path: ['nro_ord'], equals: n } }
    }
```

Sumarlo al `where` existente:

```js
    const where = {
      id_local: { in: id_local ? [id_local] : request.allowedLocalIds },
      ...nroOrdFilter,
      ...(tabla   ? { tabla }   : {}),
```

Después del `Promise.all` que trae `rows` y `total`, resolver los catálogos en 4 consultas batch y devolver los labels:

```js
    const ids = { prov: new Set(), rubcat: new Set(), metodo: new Set(), local: new Set() }
    for (const r of rows) {
      const s = r.snapshot ?? {}
      if (s.id_proveedor) ids.prov.add(s.id_proveedor)
      if (s.id_rubcat)    ids.rubcat.add(s.id_rubcat)
      if (s.id_metodo)    ids.metodo.add(s.id_metodo)
      if (s.id_local)     ids.local.add(s.id_local)
    }

    const [proveedores, rubcats, metodos, locales] = await Promise.all([
      ids.prov.size   ? fastify.db.proveedor.findMany({ where: { id: { in: [...ids.prov] } },   select: { id: true, nombre: true } }) : [],
      ids.rubcat.size ? fastify.db.rubCat.findMany({    where: { id: { in: [...ids.rubcat] } }, include: { rubro: true, categoria: true } }) : [],
      ids.metodo.size ? fastify.db.metodoPago.findMany({ where: { id: { in: [...ids.metodo] } }, select: { id: true, nombre: true } }) : [],
      ids.local.size  ? fastify.db.local.findMany({      where: { id: { in: [...ids.local] } },  select: { id: true, nombre: true } }) : [],
    ])

    const catalogos = {
      proveedores: new Map(proveedores.map(p => [p.id, p.nombre])),
      rubcats:     new Map(rubcats.map(rc => [rc.id, `${rc.rubro?.nombre ?? '—'} / ${rc.categoria?.nombre ?? '—'}`])),
      metodos:     new Map(metodos.map(m => [m.id, m.nombre])),
      locales:     new Map(locales.map(l => [l.id, l.nombre])),
    }

    const data = rows.map(r => ({ ...r, snapshot_labels: etiquetarSnapshot(r.snapshot, catalogos) }))

    return { data, total, page: Number(page), limit: Number(limit) }
```

Reemplaza el `return { data: rows, ... }` anterior. El `snapshot` original se sigue devolviendo intacto — `snapshot_labels` se agrega al lado, sin pisarlo.

- [ ] **Step 11: Verificar el endpoint con curl**

Con el backend y el proxy arriba (hace falta un JWT de super_admin, porque la ruta lo exige):

```bash
curl -s "http://localhost:3000/api/activity-log?limit=2" -H "Authorization: Bearer $TOKEN" | head -c 800
```

Esperado: cada evento trae `snapshot_labels` con nombres legibles. Y el filtro:

```bash
curl -s "http://localhost:3000/api/activity-log?nro_ord=OP-101" -H "Authorization: Bearer $TOKEN" | head -c 400
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/api/activity-log?nro_ord=abc" -H "Authorization: Bearer $TOKEN"
```

Esperado: el primero devuelve solo eventos de la OP-101; el segundo devuelve `400`.

**Si el filtro por `path` de Prisma no funciona** (devuelve vacío con datos que deberían matchear), usar el fallback del spec: reemplazar el `nroOrdFilter` por un `$queryRaw` con `(snapshot->>'nro_ord')::int = $1`. Verificarlo acá, no asumirlo.

- [ ] **Step 12: Mostrar los nombres en el frontend**

En `ActivityLog.jsx`, `snapshotRows` pasa a recibir los labels y usarlos en lugar de los ids:

```jsx
function snapshotRows(s, labels) {
  if (!s) return []
  const l = labels ?? {}
  return [
    ['Nro Orden',    s.nro_ord != null ? `OP-${s.nro_ord}` : '—'],
    ['Fecha',        fmtDate(s.fecha)],
    ['Proveedor',    l.proveedor ?? '—'],
    ['Rubro/Cat',    l.rubcat    ?? '—'],
    ['Tipo',         l.tipo      ?? '—'],
    ['PV',           s.pv ?? '—'],
    ['Nro',          s.nro ?? '—'],
    ['Neto',         fmt$(s.importe_neto)],
    ['Descuento',    fmt$(s.descuento)],
    ['Importe',      fmt$(s.importe)],
    ['Método',       l.metodo ?? '—'],
    ['Dirección',    s.ingresa_egreso != null ? (s.ingresa_egreso ? 'Ingreso' : 'Egreso') : '—'],
    ['Estado Op.',   s.estado_op || '—'],
    ['Pagado',       s.pagado ? 'Sí' : 'No'],
    ['Fecha Pago',   fmtDateArg(s.fecha_pago)],
    ['Período',      fmtDate(s.periodo)],
    ['Local',        l.local ?? '—'],
    ['Observaciones', s.observaciones || '—'],
  ]
}
```

Y en el render del detalle expandido, pasar el segundo argumento:

```jsx
                            {snapshotRows(ev.snapshot, ev.snapshot_labels).map(([label, val]) => (
```

- [ ] **Step 13: Agregar el buscador de OP al frontend**

En `FILTER_INIT`:

```jsx
const FILTER_INIT = { desde: '', hasta: '', id_user: '', accion: '', nro_ord: '' }
```

En `buildParams`:

```jsx
    ...(filters.nro_ord.trim() ? { nro_ord: filters.nro_ord.trim() } : {}),
```

El control, al final de la fila de filtros:

```jsx
        <div className="form-group" style={{ margin: 0 }}>
          <label className="form-label">OP</label>
          <div className="form-input-wrap">
            <input
              placeholder="101 u OP-101"
              value={filters.nro_ord}
              onChange={e => setFilter('nro_ord', e.target.value)}
              style={{ maxWidth: 140 }}
            />
          </div>
        </div>
```

El backend devuelve `400` con un mensaje claro si lo tipeado no es un número, y el `catch` que ya existe en el `useEffect` lo muestra con `notify`. Para que el mensaje sea el del backend y no el genérico, cambiar ese `catch`:

```jsx
      .catch((err) => {
        if (ctrl.signal.aborted) return
        notify(err.response?.data?.error || 'Error al cargar el log de actividad', 'error')
      })
```

- [ ] **Step 14: Verificar en la app**

En la pantalla Actividad (requiere usuario super_admin):

1. Expandir un evento → **Proveedor, Rubro/Cat, Método y Local muestran nombres**, no UUIDs.
2. Si algún evento tiene un proveedor borrado → dice `— (no existe)`.
3. Un evento de un pago tipo DC → el campo Tipo dice `DC (1)` o `DC (2)`.
4. Escribir `101` en el buscador de OP → solo quedan los eventos de esa OP.
5. Escribir `OP-101` → mismo resultado.
6. Escribir `abc` → aparece el aviso "El buscador de OP espera un número…", no una lista vacía sin explicación.
7. Borrar el buscador → vuelven todos los eventos.
8. Combinar el buscador de OP con un filtro de usuario o acción → se aplican los dos.

- [ ] **Step 15: Commit**

```bash
git add backend/src/lib/nroOrd.js backend/src/lib/nroOrd.test.js \
        backend/src/lib/snapshotLabels.js backend/src/lib/snapshotLabels.test.js \
        backend/src/routes/activity_log.js backend/src/routes/pagos.js \
        frontend/src/pages/activity-log/ActivityLog.jsx
git commit -m "feat(actividad): nombres reales en el detalle de la OP y buscador de OPs

El detalle mostraba los UUIDs crudos del snapshot (proveedor, rubcat,
metodo, local). Ahora el backend los resuelve en 4 consultas batch por
pagina y devuelve snapshot_labels; un id que ya no existe se marca como
tal en vez de mostrar el UUID. Se agrega buscador por numero de OP, que
acepta 101 y OP-101 con el mismo parser que ya usaba pagos (extraido a
lib/nroOrd.js para no duplicar la regla)."
```

---

### Task 6: Exportación con impuestos y totales

El `.xlsx` pasa a tener una columna por tipo de impuesto presente y una fila `TOTAL` al final. Los impuestos no vienen hoy en la respuesta de `GET /pagos`, así que hay que pedirlos explícitamente solo para el export.

**Requiere la Tarea 3 hecha** (ambas tocan `PAGO_CSV_COLUMNS`).

**Files:**
- Create: `frontend/src/lib/exportPagos.js`
- Create: `frontend/src/lib/exportPagos.test.js`
- Modify: `frontend/package.json` (agregar script `test`)
- Modify: `backend/src/routes/pagos.js` (`GET /`, `include` en `:266-272`)
- Modify: `frontend/src/lib/excel.js` (`downloadExcel` en `:18`)
- Modify: `frontend/src/pages/pagos/PagoList.jsx` (`PAGO_CSV_COLUMNS` en `:168-189`; `exportCsv` en `:916-927`)

**Interfaces:**
- Consumes: `GET /pagos` con `limit=0`.
- Produces:
  - `GET /pagos` acepta `include_impuestos=true` y agrega `impuestos: [{ id, tipo, monto }]` a cada pago.
  - `tiposImpuestoPresentes(pagos) => string[]` — tipos presentes, ordenados según el enum `TipoImpuesto`.
  - `columnasImpuesto(tipos) => Array<{ label, get }>` — columnas en el formato que ya usa `downloadExcel`.
  - `filaTotales(pagos, columns) => Array<string|number>` — fila alineada a `columns`.
  - `downloadExcel(filename, rows, columns, sheetName, totalsRow?)` — parámetro opcional nuevo, retrocompatible.

- [ ] **Step 1: Habilitar tests de módulos puros en el frontend**

El frontend no tiene runner. Los módulos de `src/lib/` son JS puro sin JSX, así que Node los corre sin transpilar. En `frontend/package.json`, agregar al bloque `scripts`:

```json
    "test": "node --test src/lib",
```

Se apunta a `src/lib` y no a la raíz para que el runner nunca intente ejecutar archivos con JSX.

- [ ] **Step 2: Escribir los tests del armado del export**

Crear `frontend/src/lib/exportPagos.test.js`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { tiposImpuestoPresentes, columnasImpuesto, filaTotales } from './exportPagos.js'

const pagos = [
  { importe_neto: 1000, importe: 1210, impuestos: [{ tipo: 'IVA21', monto: 210 }] },
  { importe_neto: 2000, importe: 2370, impuestos: [{ tipo: 'IVA21', monto: 420 }, { tipo: 'RETENCION', monto: -50 }] },
]

test('tiposImpuestoPresentes devuelve solo los tipos que aparecen', () => {
  assert.deepEqual(tiposImpuestoPresentes(pagos), ['IVA21', 'RETENCION'])
})

test('tiposImpuestoPresentes respeta el orden del enum, no el de aparicion', () => {
  const rows = [{ impuestos: [{ tipo: 'RETENCION', monto: 1 }, { tipo: 'IVA10', monto: 2 }, { tipo: 'IVA21', monto: 3 }] }]
  assert.deepEqual(tiposImpuestoPresentes(rows), ['IVA21', 'IVA10', 'RETENCION'])
})

test('tiposImpuestoPresentes devuelve vacio si no hay impuestos', () => {
  assert.deepEqual(tiposImpuestoPresentes([{ importe: 100 }]), [])
  assert.deepEqual(tiposImpuestoPresentes([{ importe: 100, impuestos: [] }]), [])
})

test('columnasImpuesto suma los montos del mismo tipo en un pago', () => {
  const [col] = columnasImpuesto(['IVA21'])
  assert.equal(col.label, 'IVA21')
  assert.equal(col.get({ impuestos: [{ tipo: 'IVA21', monto: 100 }, { tipo: 'IVA21', monto: 50 }] }), 150)
})

test('columnasImpuesto devuelve 0 (no vacio) si el pago no tiene ese impuesto', () => {
  const [col] = columnasImpuesto(['IVA21'])
  assert.equal(col.get({ impuestos: [] }), 0)
  assert.equal(col.get({}), 0)
})

test('filaTotales suma las columnas numericas y deja en blanco las de texto', () => {
  const columns = [
    { label: 'OP',      get: (p) => p.op },
    { label: 'Neto',    get: (p) => p.importe_neto ?? '' },
    { label: 'IVA21',   get: (p) => (p.impuestos ?? []).filter(i => i.tipo === 'IVA21').reduce((a, i) => a + Number(i.monto), 0) },
    { label: 'Importe', get: (p) => p.importe ?? '' },
  ]
  const fila = filaTotales(pagos, columns)
  assert.equal(fila[0], 'TOTAL')
  assert.equal(fila[1], 3000)
  assert.equal(fila[2], 630)
  assert.equal(fila[3], 3580)
})

test('filaTotales pone TOTAL en la primera celda aunque esa columna sea numerica', () => {
  const columns = [{ label: 'Neto', get: (p) => p.importe_neto }]
  assert.equal(filaTotales(pagos, columns)[0], 'TOTAL')
})

test('filaTotales no suma columnas de fecha ni de texto', () => {
  const columns = [
    { label: 'OP',    get: () => 'OP-1' },
    { label: 'Fecha', get: () => '15/07/2026' },
  ]
  assert.deepEqual(filaTotales(pagos, columns), ['TOTAL', ''])
})
```

- [ ] **Step 3: Correr los tests y verificar que fallan**

```bash
cd frontend && npm test
```

Esperado: FAIL — `Cannot find module './exportPagos.js'`.

- [ ] **Step 4: Implementar el módulo**

Crear `frontend/src/lib/exportPagos.js`:

```js
// Armado de las columnas de impuesto y la fila de totales del export de pagos.
// Separado de PagoList para poder testearlo con node --test (es JS puro).

// Orden del enum TipoImpuesto en schema.prisma. Se respeta para que el archivo
// tenga siempre las mismas columnas en el mismo orden entre exports.
const ORDEN_TIPOS = ['IVA21', 'IVA27', 'IVA10', 'RETENCION', 'PERCEPCION']

function montoDe(pago, tipo) {
  return (pago.impuestos ?? [])
    .filter((i) => i.tipo === tipo)
    .reduce((acc, i) => acc + Number(i.monto ?? 0), 0)
}

export function tiposImpuestoPresentes(pagos) {
  const presentes = new Set()
  for (const p of pagos) {
    for (const imp of p.impuestos ?? []) presentes.add(imp.tipo)
  }
  const conocidos = ORDEN_TIPOS.filter((t) => presentes.has(t))
  // Un tipo nuevo en la base que todavia no este en ORDEN_TIPOS igual se
  // exporta, al final y alfabetico, en vez de desaparecer del archivo.
  const desconocidos = [...presentes].filter((t) => !ORDEN_TIPOS.includes(t)).sort()
  return [...conocidos, ...desconocidos]
}

export function columnasImpuesto(tipos) {
  return tipos.map((tipo) => ({
    label: tipo,
    // 0 y no '' para que la columna sume bien en Excel.
    get: (pago) => montoDe(pago, tipo),
  }))
}

export function filaTotales(pagos, columns) {
  return columns.map((col, i) => {
    if (i === 0) return 'TOTAL'
    const valores = pagos.map((p) => col.get(p))
    const numericos = valores.filter((v) => v !== '' && v != null && !isNaN(Number(v)))
    // Si ninguna celda de la columna es numerica, es texto o fecha: no se suma.
    if (numericos.length === 0) return ''
    return numericos.reduce((acc, v) => acc + Number(v), 0)
  })
}
```

- [ ] **Step 5: Correr los tests y verificar que pasan**

```bash
cd frontend && npm test
```

Esperado: 8 tests en verde.

- [ ] **Step 6: Devolver los impuestos desde el backend, solo para el export**

En `pagos.js`, `GET /`, agregar `include_impuestos` al destructuring de la query:

```js
      audit, ingresa_egreso, id_metodo, nro_ord, cmv_quick, q, observaciones,
      include_impuestos,
      sort_field = 'fecha', sort_dir = 'desc',
      page = 1, limit = 50
    } = request.query
```

Y hacerlo condicional en el `include` del `findMany`:

```js
        include: {
          proveedor:   { select: { id: true, nombre: true, razon_social: true } },
          rubcat:      { include: { rubro: true, categoria: true } },
          metodo_pago: true,
          local:       { select: { id: true, nombre: true } },
          creador:     { select: { id: true, nombre: true } },
          // Solo para el export: la tabla no los necesita y son N filas mas
          // por pago en cada pagina.
          ...(include_impuestos === 'true'
            ? { impuestos: { select: { id: true, tipo: true, monto: true } } }
            : {}),
        },
```

- [ ] **Step 7: Aceptar la fila de totales en `downloadExcel`**

En `frontend/src/lib/excel.js`, agregar el parámetro opcional al final para no romper los otros exports que ya la usan:

```js
export async function downloadExcel(filename, rows, columns, sheetName = 'Datos', totalsRow = null) {
  const XLSX = await import('xlsx')
  const header = columns.map((c) => c.label)
  const body = rows.map((row) => columns.map((c) => coerce(c.get(row))))
  const aoa = [header, ...body, ...(totalsRow ? [totalsRow] : [])]
```

El resto de la función queda igual: el cálculo de `!cols` usa `body`, que no incluye la fila de totales, y eso está bien — un ancho de columna dominado por la palabra `TOTAL` no aporta.

Nota: la edición community de SheetJS no aplica estilos, así que la fila **no puede ir en negrita**. Se distingue por la etiqueta `TOTAL`.

- [ ] **Step 8: Mover la columna Observaciones en `PAGO_CSV_COLUMNS`**

El comentario de esa constante dice que son "las mismas columnas que se ven en la tabla", y en la Tarea 3 la columna quedó después de Método. Mover la entrada `Observaciones` del final a justo después de `Método`:

```js
  { label: 'Método',      get: (p) => p.metodo_pago?.nombre || '' },
  { label: 'Observaciones', get: (p) => p.observaciones || '' },
  { label: 'Cashflow',    get: (p) => p.cashflow ? fmtDate(p.cashflow) : '' },
```

Y borrar la entrada `Observaciones` que estaba al final de la lista, después de `Local`.

- [ ] **Step 9: Armar el export con impuestos y totales**

En `PagoList.jsx`, agregar el import:

```jsx
import { tiposImpuestoPresentes, columnasImpuesto, filaTotales } from '../../lib/exportPagos.js'
```

Y reemplazar `exportCsv` por:

```jsx
  const exportCsv = useCallback(async () => {
    setExporting(true)
    try {
      const { data } = await pagosApi.list({ ...buildParams(1), limit: 0, include_impuestos: 'true' })
      if (!data.data.length) { notify('No hay filas para exportar con estos filtros', 'info'); return }

      const pagos = data.data
      // Las columnas de impuesto van entre Neto e Importe.
      const idxImporte = PAGO_CSV_COLUMNS.findIndex(c => c.label === 'Importe')
      const columns = [
        ...PAGO_CSV_COLUMNS.slice(0, idxImporte),
        ...columnasImpuesto(tiposImpuestoPresentes(pagos)),
        ...PAGO_CSV_COLUMNS.slice(idxImporte),
      ]

      await downloadExcel(
        `pagos_${todayInputDate()}.xlsx`,
        pagos,
        columns,
        'Pagos',
        filaTotales(pagos, columns),
      )
    } catch {
      notify('Error al exportar Excel', 'error')
    } finally {
      setExporting(false)
    }
  }, [buildParams, notify])
```

- [ ] **Step 10: Verificar el archivo generado**

En la pantalla de Pagos, con un rango de fechas que incluya pagos con impuestos cargados, exportar a Excel y abrir el archivo:

1. Hay **una columna por cada tipo de impuesto** presente en esas filas (IVA21, RETENCION, etc.), ubicadas entre `Neto` e `Importe`.
2. Un pago **sin** un impuesto dado tiene `0` en esa columna, no vacío.
3. La columna **Observaciones** está después de `Método`, igual que en la tabla.
4. La **última fila dice `TOTAL`** en la primera celda.
5. Los totales de `Neto`, `Importe` y cada impuesto **cuadran**: seleccionar la columna en Excel y comparar la suma que muestra con el valor de la fila TOTAL.
6. Las columnas de texto y fecha están **vacías** en la fila TOTAL (no dicen "0" ni una fecha inventada).
7. Exportar con un filtro que **no** deje pagos con impuestos → el archivo sale sin columnas de impuesto y con la fila TOTAL sumando solo Neto e Importe.
8. Verificar que **la tabla en pantalla siga rápida**: el `include_impuestos` es solo del export, así que la navegación por páginas no debería cambiar.

- [ ] **Step 11: Commit**

```bash
git add frontend/src/lib/exportPagos.js frontend/src/lib/exportPagos.test.js \
        frontend/package.json frontend/src/lib/excel.js \
        backend/src/routes/pagos.js frontend/src/pages/pagos/PagoList.jsx
git commit -m "feat(pagos): el export a Excel incluye impuestos y fila de totales

Una columna por tipo de impuesto presente (entre Neto e Importe) y una
fila TOTAL al final. Los totales se suman sobre las filas ya
descargadas, no con GET /summary, para que el total del archivo cuadre
siempre con sus propias filas. Los impuestos se piden solo en el export
via include_impuestos, la tabla no los necesita. Se mueve la columna
Observaciones para que el archivo siga espejando la tabla.

La fila TOTAL no va en negrita: la edicion community de SheetJS no
aplica estilos de celda.

Se agrega script de test al frontend (node --test sobre src/lib, que es
JS puro sin JSX) para cubrir el armado de columnas y totales."
```

---

### Task 7: Estado OP "CAJA" al pagar fuera de PDP

Un pago que se paga fuera del flujo PDP queda en `estado_op = CAJA`; los que vienen de PDP conservan su estado.

**Refinamiento respecto del spec:** el spec proponía resolverlo con un `OR` sobre `estado_op` dentro del `where` de Prisma (por el problema de que `notIn` no matchea `NULL`). El plan lo hace distinto: **leer los pagos afectados primero y partir los ids en JS**. Ventajas: la regla queda en una función pura testeable en vez de escondida en un `where`, desaparece el riesgo del `NULL` en Prisma, y el endpoint puede devolver qué ids quedaron en CAJA (que el frontend necesita). El resultado observable es el mismo.

**Files:**
- Create: `backend/src/lib/estadoOp.js`
- Create: `backend/src/lib/estadoOp.test.js`
- Modify: `backend/src/routes/pagos.js` (`POST /pagar` en `:846-863`)
- Modify: `frontend/src/pages/pagos/PagoList.jsx` (`handlePagar` en `:303-315`)
- Verify only (no cambia): `frontend/src/pages/pdp/PdpDashboard.jsx` (`handlePagar` en `:589-600`, recarga con `load()`)

**Interfaces:**
- Consumes: nada.
- Produces:
  - `ESTADOS_QUE_CONSERVAN = ['PDP', 'MP_PDP']` — de `backend/src/lib/estadoOp.js`.
  - `debeQuedarEnCaja(estadoActual: string|null) => boolean`.
  - `partirIdsPorEstado(pagos: Array<{id, estado_op}>) => { idsCaja: string[], idsConservan: string[] }`.
  - `POST /pagos/pagar` devuelve `{ ok: true, count: number, ids_caja: string[] }`.

- [ ] **Step 1: Escribir el test de la regla**

Crear `backend/src/lib/estadoOp.test.js`:

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { debeQuedarEnCaja, partirIdsPorEstado } from './estadoOp.js'

test('un pago en CUENTA_CTE pasa a CAJA al pagarse', () => {
  assert.equal(debeQuedarEnCaja('CUENTA_CTE'), true)
})

test('un pago sin estado (null) pasa a CAJA al pagarse', () => {
  assert.equal(debeQuedarEnCaja(null), true)
  assert.equal(debeQuedarEnCaja(undefined), true)
})

test('un pago que ya esta en CAJA sigue en CAJA', () => {
  assert.equal(debeQuedarEnCaja('CAJA'), true)
})

test('los pagos del flujo PDP conservan su estado', () => {
  assert.equal(debeQuedarEnCaja('PDP'), false)
  assert.equal(debeQuedarEnCaja('MP_PDP'), false)
})

test('partirIdsPorEstado separa los dos grupos', () => {
  const pagos = [
    { id: 'a', estado_op: 'CUENTA_CTE' },
    { id: 'b', estado_op: 'PDP' },
    { id: 'c', estado_op: null },
    { id: 'd', estado_op: 'MP_PDP' },
    { id: 'e', estado_op: 'CAJA' },
  ]
  const { idsCaja, idsConservan } = partirIdsPorEstado(pagos)
  assert.deepEqual(idsCaja,     ['a', 'c', 'e'])
  assert.deepEqual(idsConservan, ['b', 'd'])
})

test('partirIdsPorEstado con lista vacia devuelve dos listas vacias', () => {
  assert.deepEqual(partirIdsPorEstado([]), { idsCaja: [], idsConservan: [] })
})
```

- [ ] **Step 2: Correr el test y verificar que falla**

```bash
cd backend && npm test
```

Esperado: FAIL — `Cannot find module './estadoOp.js'`.

- [ ] **Step 3: Implementar la regla**

Crear `backend/src/lib/estadoOp.js`:

```js
// Regla de estado_op al marcar un pago como pagado: si el pago no venia del
// flujo PDP, la plata salio de la caja, asi que queda en CAJA. Los que si
// venian de PDP conservan su estado para no perder esa trazabilidad.
export const ESTADOS_QUE_CONSERVAN = ['PDP', 'MP_PDP']

export function debeQuedarEnCaja(estadoActual) {
  return !ESTADOS_QUE_CONSERVAN.includes(estadoActual)
}

export function partirIdsPorEstado(pagos) {
  const idsCaja = []
  const idsConservan = []
  for (const p of pagos) {
    if (debeQuedarEnCaja(p.estado_op)) idsCaja.push(p.id)
    else idsConservan.push(p.id)
  }
  return { idsCaja, idsConservan }
}
```

- [ ] **Step 4: Correr el test y verificar que pasa**

```bash
cd backend && npm test
```

Esperado: los 6 tests de `estadoOp` en verde, más todos los anteriores.

- [ ] **Step 5: Aplicar la regla en `POST /pagar`**

En `pagos.js`, agregar el import:

```js
import { partirIdsPorEstado } from '../lib/estadoOp.js'
```

Y reemplazar el handler completo de `POST /pagar`:

```js
  // ── POST /pagar ────────────────────────────────────────────────────────
  // Flujo PDP, etapa 2: marca los pagos seleccionados como pagados,
  // registrando fecha de pago y forma de pago (método).
  //
  // Un pago que NO venia del flujo PDP queda en estado_op = CAJA: la plata
  // salio de la caja. Los que venian de PDP/MP_PDP conservan su estado.
  // Ver lib/estadoOp.js.
  fastify.post('/pagar', { preHandler: editHandler }, async (request, reply) => {
    const { ids, fecha_pago, id_metodo } = request.body
    if (!Array.isArray(ids) || ids.length === 0) {
      return reply.code(400).send({ error: 'Sin pagos seleccionados' })
    }
    if (!id_metodo) {
      return reply.code(400).send({ error: 'Forma de pago requerida' })
    }

    // Se leen antes para decidir el estado de cada uno y para devolverle al
    // frontend que ids quedaron en CAJA (asi no duplica la regla).
    const afectados = await fastify.db.pago.findMany({
      where:  { id: { in: ids }, id_local: { in: request.allowedLocalIds } },
      select: { id: true, estado_op: true }
    })
    if (afectados.length === 0) return { ok: true, count: 0, ids_caja: [] }

    const { idsCaja, idsConservan } = partirIdsPorEstado(afectados)
    const datosComunes = {
      pagado:     true,
      fecha_pago: fecha_pago ? new Date(fecha_pago) : new Date(),
      id_metodo
    }

    await fastify.db.$transaction([
      ...(idsCaja.length ? [fastify.db.pago.updateMany({
        where: { id: { in: idsCaja } },
        data:  { ...datosComunes, estado_op: 'CAJA' }
      })] : []),
      ...(idsConservan.length ? [fastify.db.pago.updateMany({
        where: { id: { in: idsConservan } },
        data:  datosComunes
      })] : []),
    ])

    return { ok: true, count: afectados.length, ids_caja: idsCaja }
  })
```

El filtro por `allowedLocalIds` se aplica en el `findMany`, así que los `updateMany` ya operan sobre ids verificados.

- [ ] **Step 6: Reflejar el estado nuevo en el panel de detalle**

En `PagoList.jsx`, `handlePagar` hace un patch optimista que no incluía `estado_op`. Usar lo que devuelve el endpoint:

```jsx
  const handlePagar = async (e) => {
    e.preventDefault()
    if (!pagarForm.id_metodo) return notify('Seleccioná un método de pago', 'error')
    setPagando(true)
    try {
      const fechaPagoIso = toUtcIsoFromDateTimeLocal(pagarForm.fecha_pago)
      const { data } = await pagosApi.pagar([pago.id], { fecha_pago: fechaPagoIso, id_metodo: pagarForm.id_metodo })
      notify('Pago registrado', 'success')
      setPagarOpen(false)
      onPatch?.(pago.id, {
        pagado: true,
        fecha_pago: fechaPagoIso,
        id_metodo: pagarForm.id_metodo,
        ...(data?.ids_caja?.includes(pago.id) ? { estado_op: 'CAJA' } : {}),
      })
    } catch { notify('Error al pagar', 'error') }
    finally { setPagando(false) }
  }
```

- [ ] **Step 7: Confirmar que el pago en lote del dashboard de PDP no necesita cambios**

`PdpDashboard.handlePagar` (`:589-600`) llama a `load()` después de pagar, así que recarga los pagos desde el backend y el `estado_op` nuevo llega solo. Tampoco se rompe por la respuesta nueva: usa `data.count`, que se sigue devolviendo.

**No hay que editar este archivo.** Solo verificar que sigue así (que nadie lo cambió entre la escritura del plan y su ejecución):

```bash
sed -n '589,600p' frontend/src/pages/pdp/PdpDashboard.jsx
```

Esperado: el bloque termina en `load()` dentro del `try`. Si ya no llama a `load()` y en cambio actualiza filas en memoria, aplicar el mismo criterio del Step 6 con `data.ids_caja`.

- [ ] **Step 8: Verificar en la app contra la base**

Con los 3 servicios arriba. **Antes de tocar nada**, anotar el `estado_op` de los pagos de prueba.

1. Un pago en **Cuenta Corriente** no pagado → abrir el detalle, "Pagar", elegir método y confirmar. El badge de estado pasa a **CAJA** sin recargar la página.
2. Recargar la página → sigue en CAJA (o sea, se persistió, no fue solo el patch optimista).
3. Un pago **mandado a PDP** → pagarlo. El estado **sigue en PDP**, no cambia a CAJA.
4. Un pago con estado **vacío** (si hay alguno en la base) → al pagarlo queda en CAJA. Este es el caso que el `notIn` de Prisma se habría comido.
5. Pago en **lote** desde el dashboard de PDP → los pagos que estaban en PDP conservan PDP; si el lote incluye alguno en Cuenta Corriente, ese queda en CAJA.
6. Verificar con SQL que no se tocó nada de más:

```sql
SELECT estado_op, pagado, COUNT(*) FROM pagos GROUP BY estado_op, pagado ORDER BY 1, 2;
```

- [ ] **Step 9: Commit**

```bash
git add backend/src/lib/estadoOp.js backend/src/lib/estadoOp.test.js \
        backend/src/routes/pagos.js frontend/src/pages/pagos/PagoList.jsx
git commit -m "feat(pagos): estado_op pasa a CAJA al pagar fuera de PDP

POST /pagar marcaba pagado sin tocar estado_op. Ahora un pago que no
venia del flujo PDP queda en CAJA (la plata salio de la caja) y los que
venian de PDP/MP_PDP conservan su estado.

El endpoint lee los pagos afectados y parte los ids en JS en vez de
resolverlo con un OR en el where: la regla queda en una funcion pura con
tests, se evita que un notIn de Prisma se coma los estado_op en NULL, y
puede devolver ids_caja para que el frontend no duplique la regla."
```

---

### Task 8: Observaciones en Arqueo

Único ítem que toca el esquema de la base. El campo es informativo: **no participa de ningún cálculo** (`total`, `ingresos`, `gastos`, `comprobacion` no lo miran).

**Files:**
- Modify: `backend/prisma/schema.prisma` (`model Arqueo`)
- Modify: `backend/src/routes/arqueo.js` (`POST /` en `:183-228`; `PUT /:id` en `:234-275`)
- Modify: `frontend/src/pages/arqueo/ArqueoList.jsx` (`ArqueoCreatePanel` en `:26-105`; `ArqueoEditPanel` en `:155-223`; detalle del drawer en `:318-339`; tabla en `:434-450`)

**Interfaces:**
- Consumes: nada.
- Produces: `Arqueo.observaciones` (string, nullable). `POST /arqueo` y `PUT /arqueo/:id` aceptan `observaciones` en el body. `arqueoApi` no necesita cambios: `create` y `update` pasan el body completo.

- [ ] **Step 1: Agregar el campo al schema**

En `backend/prisma/schema.prisma`, `model Arqueo`, después de `comprobacion`:

```prisma
  comprobacion Decimal  @db.Decimal(12, 2)
  observaciones String?
  created_by   String?
```

- [ ] **Step 2: GATE — mostrar el drift al usuario y esperar aprobación**

**No aplicar nada a la base todavía.** Con el proxy arriba, generar el diff entre el schema y la base real:

```bash
cd backend && npx prisma migrate diff \
  --from-url "$DATABASE_URL" \
  --to-schema-datamodel prisma/schema.prisma \
  --script
```

Mostrarle al usuario la salida completa y explicar:

- Qué se espera ver: el `ALTER TABLE arqueos ADD COLUMN observaciones`.
- **Qué más aparece**: cualquier otro drift acumulado, en particular el modelo `MultiMoneda` que CLAUDE.md marca como posiblemente no aplicado. Ese drift **no** se toca en esta tarea.
- Por qué no se usa `prisma db push`: sincronizaría todo el esquema, arrastrando ese drift.

**Esperar confirmación explícita del usuario antes del Step 3.** Si no la da, dejar los Steps 3-4 sin hacer y seguir con el 5 (el código de frontend/backend se puede escribir igual, solo no funcionará hasta que la columna exista).

- [ ] **Step 3: Aplicar el `ALTER TABLE` (solo con aprobación)**

```sql
ALTER TABLE arqueos ADD COLUMN observaciones text;
```

Aditivo y nullable: los arqueos existentes quedan en `NULL`. No rompe el ETL de `dcsmart-analisis` (agregar una columna no invalida un `SELECT`).

Verificar que quedó:

```sql
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'arqueos' AND column_name = 'observaciones';
```

Esperado: una fila, `text`, `YES`.

- [ ] **Step 4: Regenerar el cliente de Prisma**

```bash
cd backend && npx prisma generate
```

- [ ] **Step 5: Aceptar y guardar el campo en el backend**

En `arqueo.js`, `POST /`, agregar al destructuring y al `create`:

```js
    const { id_local, fecha, caja_fuerte, cofre, adicion, detalles, observaciones } = request.body
```

```js
        comprobacion: String(comprobacion),
        observaciones: observaciones?.trim() || null,
        created_by: request.user.id,
```

En `PUT /:id`, lo mismo:

```js
    const { fecha, caja_fuerte, cofre, adicion, detalles, observaciones } = request.body
```

```js
        comprobacion: String(comprobacion),
        observaciones: observaciones?.trim() || null,
```

No se agrega a la validación de campos requeridos: es opcional.

- [ ] **Step 6: Agregar el campo al formulario de creación**

En `ArqueoCreatePanel`, el estado:

```jsx
  const [adicion,    setAdicion]    = useState('')
  const [observaciones, setObservaciones] = useState('')
```

Enviarlo en el submit:

```jsx
        adicion: parseFloat(adicion) || 0,
        observaciones: observaciones.trim() || null,
        detalles: pendingDetalles.map(d => ({
```

Y el control, después del bloque de Adición (cambiando el `margin: 0` de Adición por `margin: '0 0 0.9rem'`, ya que deja de ser el último):

```jsx
      <div className="form-group" style={{ margin: 0 }}>
        <label className="form-label">Observaciones</label>
        <div className="form-input-wrap">
          <textarea
            rows={2}
            placeholder="Opcional"
            value={observaciones}
            onChange={e => setObservaciones(e.target.value)}
          />
        </div>
      </div>
```

- [ ] **Step 7: Agregar el campo al formulario de edición**

En `ArqueoEditPanel`, el estado precargado con lo que ya tiene el arqueo:

```jsx
  const [adicion,    setAdicion]    = useState(String(arqueo.adicion))
  const [observaciones, setObservaciones] = useState(arqueo.observaciones ?? '')
```

En el submit:

```jsx
        adicion: parseFloat(adicion) || 0,
        observaciones: observaciones.trim() || null,
        detalles: detalles.map(d => ({ id_tipo: d.id_tipo || null, monto: parseFloat(d.monto) || 0 }))
```

Y el mismo control del Step 6 después del bloque de Adición (ajustando igual el `margin` de Adición).

- [ ] **Step 8: Mostrar las observaciones en el detalle**

En el drawer de detalle, después de la fila de Comprobación y antes del cierre del `<div className="drawer-detail">`:

```jsx
        {arqueo.observaciones && (
          <div className="drawer-detail-row">
            <span className="drawer-detail-key">Observaciones</span>
            <span className="drawer-detail-val" style={{ whiteSpace: 'pre-wrap', textAlign: 'right' }}>
              {arqueo.observaciones}
            </span>
          </div>
        )}
```

Se muestra solo si tiene contenido, para no agregar una fila vacía a todos los arqueos viejos.

- [ ] **Step 9: Agregar la columna a la tabla**

En el `<thead>`, entre Comprobación y Auditado:

```jsx
                <th>Fecha</th><th>Caja fuerte</th><th>Cofre</th><th>Adición</th>
                <th>Total</th><th>Comprobación</th><th>Observaciones</th><th>Auditado</th><th></th>
```

Y el `<td>` correspondiente en la misma posición dentro del `map`, truncado con tooltip como la columna de pagos:

```jsx
                    <td
                      style={{ maxWidth: 180, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap', fontSize: 12 }}
                      title={a.observaciones || ''}
                    >
                      {a.observaciones || <span className="td-muted">—</span>}
                    </td>
```

- [ ] **Step 10: Verificar en la app**

1. Crear un arqueo **con** observaciones → se guarda; aparece en la tabla y en el detalle.
2. Crear un arqueo **sin** observaciones → se guarda igual; la tabla muestra `—` y el detalle no muestra la fila.
3. Editar un arqueo existente y agregarle observaciones → se guarda y se ve.
4. Editar un arqueo **borrando** el texto → queda vacío, sin error.
5. Verificar que `total`, `ingresos`, `gastos` y `comprobacion` **no cambiaron** al editar solo las observaciones. Es el punto importante: el campo es informativo y no debe alterar la cuadratura. Anotar los valores antes y después.
6. Una observación larga se trunca en la tabla con tooltip, y en el detalle se ve completa respetando los saltos de línea.
7. Los arqueos **anteriores** a la migración siguen abriendo bien y muestran `—`.

- [ ] **Step 11: Commit**

```bash
git add backend/prisma/schema.prisma backend/src/routes/arqueo.js \
        frontend/src/pages/arqueo/ArqueoList.jsx
git commit -m "feat(arqueo): campo de observaciones

Campo informativo, nullable: no participa de total, ingresos, gastos ni
comprobacion. Se agrega a los formularios de creacion y edicion, al
detalle del drawer y a la tabla (truncado con tooltip).

La columna se aplico con un ALTER TABLE puntual y no con prisma db push,
que habria arrastrado el drift pendiente de MultiMoneda."
```

---

## Cierre

- [ ] **Correr los tests de los dos lados**

```bash
cd backend && npm test
cd ../frontend && npm test && npm run lint
```

Esperado: backend con los 8 originales + 18 nuevos (6 `nroOrd` + 6 `snapshotLabels` + 6 `estadoOp`); frontend con 8 (`exportPagos`) y lint sin errores nuevos.

- [ ] **Revisar el diff completo de la branch**

```bash
git log --oneline dev..DEV-35
git diff dev...DEV-35 --stat
```

Esperado: 8 commits, uno por tarea.

- [ ] **No pushear ni abrir PR sin pedirlo**

`dev` despliega al mismo backend de Cloud Run que `master`. Preguntarle al usuario antes de pushear `DEV-35` o abrir el PR, y confirmar el base branch con `gh pr view <N> --json baseRefName` si se crea.
