# Arqueo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Backend completo para el arqueo (conteo físico de efectivo por local vs lo acumulado en el
sistema desde el arqueo anterior), con la fórmula de comprobación ya definida en la spec.

**Architecture:** Dos modelos Prisma nuevos (`Arqueo`, `ArqueoDetalle`, reusando el catálogo
`DetalleTipo` ya existente), un módulo de permisos nuevo (`arqueo`), y rutas backend siguiendo el
mismo patrón ya establecido en `caja.js`/`pdp.js` (`viewHandler`/`createHandler` vía
`fastify.can('arqueo', ...)`). El cálculo de `ingresos`/`gastos`/`comprobacion` se hace en el backend
al crear un arqueo, consultando `Caja`/`Pago` del mismo local en el rango de fechas desde el arqueo
anterior. Esta iteración es solo backend — el frontend se define en una ronda de brainstorming
posterior (fuera de alcance de este plan, según la spec).

**Tech Stack:** Prisma/Postgres, Fastify.

## Global Constraints

- Montos siempre en `Decimal(12,2)`, consistente con el resto del schema.
- IDs de modelos nuevos: UUID v4 (`@default(uuid())`).
- `prisma db push` es el mecanismo de schema en este repo.
- Permisos: módulo nuevo `arqueo`, no reusar el de `caja` (spec explícita).
- `ingresos`/`gastos` son snapshots al momento de crear el arqueo — no se recalculan después.
- **NO correr `node prisma/seed.js` completo contra la base real** (script destructivo documentado,
  ver `feedback_seed-js-destructivo.md` en la memoria del proyecto) — para aplicar el módulo/permisos
  nuevos contra la base compartida real, se usa un script chico de un solo uso que solo hace
  `upsert` de `Module`/`RolePermission` (nunca `deleteMany` de usuarios/roles).
- `master` es producción real, no se toca sin confirmación explícita del usuario (esta rama no toca
  master).

---

## Mapa de archivos

```
dcsmart/backend/
├── prisma/
│   ├── schema.prisma           (modificar: model Arqueo, model ArqueoDetalle, relaciones)
│   └── seed.js                 (modificar: agrega 'arqueo' a MODULES/MATRIX — fuente de verdad
│                                 para futuros onboardings, NO se corre completo contra la base real)
├── scripts/
│   └── seed-arqueo-permisos.cjs (nuevo: aplica SOLO el módulo 'arqueo' + permisos contra la base
│                                 real, sin tocar nada más — no versionado, mismo criterio que otros
│                                 scripts one-off del proyecto)
└── src/
    ├── routes/arqueo.js        (nuevo: POST /, GET /, GET /:id)
    └── server.js               (modificar: registra arqueoRoutes)
```

---

### Task 1: Schema — `Arqueo`, `ArqueoDetalle` y módulo de permisos

**Files:**
- Modify: `dcsmart/backend/prisma/schema.prisma`
- Modify: `dcsmart/backend/prisma/seed.js`

**Interfaces:**
- Produces: modelos `Arqueo` (`id, id_local, fecha, caja_fuerte, cofre, adicion, total, ingresos,
  gastos, comprobacion, created_by, created_at`) y `ArqueoDetalle` (`id, id_arqueo, id_tipo, nombre,
  monto, created_at`), relación inversa `DetalleTipo.arqueo_detalles`, módulo de permisos `arqueo` en
  `seed.js` — consumidos por Task 2 (rutas backend) y Task 3 (script de permisos).

- [ ] **Step 1: Agregar los modelos al schema**

En `dcsmart/backend/prisma/schema.prisma`, agregar (por ejemplo después del modelo `DetalleTipo`,
línea ~292):

```prisma
model Arqueo {
  id           String   @id @default(uuid())
  id_local     String
  fecha        DateTime
  caja_fuerte  Decimal  @db.Decimal(12, 2)
  cofre        Decimal  @db.Decimal(12, 2)
  adicion      Decimal  @db.Decimal(12, 2)
  total        Decimal  @db.Decimal(12, 2)
  ingresos     Decimal  @db.Decimal(12, 2)
  gastos       Decimal  @db.Decimal(12, 2)
  comprobacion Decimal  @db.Decimal(12, 2)
  created_by   String?
  created_at   DateTime @default(now())

  local    Local           @relation(fields: [id_local], references: [id])
  creador  User?           @relation("ArqueoCreatedBy", fields: [created_by], references: [id])
  detalles ArqueoDetalle[]

  @@index([id_local, fecha])
  @@map("arqueos")
}

model ArqueoDetalle {
  id         String   @id @default(uuid())
  id_arqueo  String
  id_tipo    String?
  nombre     String?
  monto      Decimal  @db.Decimal(12, 2)
  created_at DateTime @default(now())

  arqueo       Arqueo       @relation(fields: [id_arqueo], references: [id])
  detalle_tipo DetalleTipo? @relation(fields: [id_tipo], references: [id])

  @@map("arqueo_detalles")
}
```

- [ ] **Step 2: Agregar las relaciones inversas en `Local`, `User` y `DetalleTipo`**

En `model Local` (línea ~29), agregar `arqueos Arqueo[]` junto a `pdps`:

```prisma
  cajas         Caja[]
  pagos         Pago[]
  pdps          Pdp[]
  arqueos       Arqueo[]
  local_access  UserLocalAccess[]
  detalle_tipos DetalleTipo[]
```

En `model User` (línea ~51), agregar `arqueos_creados Arqueo[] @relation("ArqueoCreatedBy")` junto a
`pdps_creados`:

```prisma
  cajas_creadas    Caja[]            @relation("CajaCreatedBy")
  pagos_creados    Pago[]            @relation("PagoCreatedBy")
  pdps_creados     Pdp[]             @relation("PdpCreatedBy")
  arqueos_creados  Arqueo[]          @relation("ArqueoCreatedBy")
  local_access     UserLocalAccess[]
```

En `model DetalleTipo` (línea ~278), agregar `arqueo_detalles ArqueoDetalle[]` junto a `detalles`:

```prisma
  app             App             @relation(fields: [id_app], references: [id])
  local           Local?          @relation(fields: [id_local], references: [id])
  detalles        CajaDetalle[]
  arqueo_detalles ArqueoDetalle[]
```

- [ ] **Step 3: Aplicar el schema a la base y regenerar el cliente**

Con el túnel `cloud-sql-proxy` activo en el puerto 5433 (parar el backend local si está corriendo,
para que `prisma generate` no encuentre el archivo bloqueado en Windows):

```bash
cd dcsmart/backend
npx prisma db push
npx prisma generate
```
Expected: `Your database is now in sync with your Prisma schema.` y `Generated Prisma Client`.

- [ ] **Step 4: Agregar el módulo `arqueo` a `seed.js` (fuente de verdad, no se corre completo)**

En `dcsmart/backend/prisma/seed.js`, agregar `'arqueo'` a `MODULES` (línea 10-13):

```js
const MODULES = [
  'caja', 'caja_movimientos', 'pagos', 'proveedores',
  'rubros', 'categorias', 'metodos_pago', 'usuarios', 'apps', 'locales', 'reportes', 'arqueo'
]
```

Y agregar la fila `arqueo` a cada rol dentro de `MATRIX` (línea 15-51), mismo nivel de permiso que
`caja` para cada rol (super_admin y dcsmart control total, admin/cajero pueden ver y crear pero no
editar/borrar):

```js
const MATRIX = {
  super_admin: {
    caja: [T,T,T,T], caja_movimientos: [T,T,T,T], pagos: [T,T,T,T],
    proveedores: [T,T,T,T], rubros: [T,T,T,T], categorias: [T,T,T,T],
    metodos_pago: [T,T,T,T], usuarios: [T,T,T,T], apps: [T,T,T,T], locales: [T,T,T,T],
    reportes: [T,T,T,T], arqueo: [T,T,T,T],
  },
  dcsmart: {
    caja: [T,T,T,T], caja_movimientos: [T,T,T,T], pagos: [T,T,T,T],
    proveedores: [T,T,T,T], rubros: [T,F,F,F], categorias: [T,F,F,F],
    metodos_pago: [T,T,T,T], usuarios: [F,F,F,F], apps: [T,F,F,F], locales: [T,F,F,F],
    reportes: [T,F,F,F], arqueo: [T,T,T,T],
  },
  admin: {
    caja: [T,T,T,F], caja_movimientos: [T,T,T,F], pagos: [T,T,T,F],
    proveedores: [T,T,T,F], rubros: [T,F,F,F], categorias: [T,F,F,F],
    metodos_pago: [T,F,F,F], usuarios: [F,F,F,F], apps: [F,F,F,F], locales: [F,F,F,F],
    reportes: [F,F,F,F], arqueo: [T,T,F,F],
  },
  cajero: {
    caja: [T,T,F,F], caja_movimientos: [T,T,F,F], pagos: [T,T,F,F],
    proveedores: [T,F,F,F], rubros: [T,F,F,F], categorias: [T,F,F,F],
    metodos_pago: [T,F,F,F], usuarios: [F,F,F,F], apps: [F,F,F,F], locales: [F,F,F,F],
    reportes: [F,F,F,F], arqueo: [T,T,F,F],
  },
  reportes: {
    caja: [F,F,F,F], caja_movimientos: [F,F,F,F], pagos: [F,F,F,F],
    proveedores: [F,F,F,F], rubros: [F,F,F,F], categorias: [F,F,F,F],
    metodos_pago: [F,F,F,F], usuarios: [F,F,F,F], apps: [F,F,F,F], locales: [F,F,F,F],
    reportes: [T,F,F,F], arqueo: [F,F,F,F],
  },
}
```

- [ ] **Step 5: Verificar sintaxis de `seed.js`**

```bash
cd dcsmart/backend
node --check prisma/seed.js
```
Expected: sin output (sin errores).

- [ ] **Step 6: Commit**

```bash
cd dcsmart
git add backend/prisma/schema.prisma backend/prisma/seed.js
git commit -m "Arqueo: agrega modelos Arqueo/ArqueoDetalle y modulo de permisos arqueo"
```

---

### Task 2: Backend — rutas `/api/arqueo` (crear, listar, ver detalle)

**Files:**
- Create: `dcsmart/backend/src/routes/arqueo.js`
- Modify: `dcsmart/backend/src/server.js`

**Interfaces:**
- Consumes: modelos `Arqueo`/`ArqueoDetalle` (Task 1), `fastify.authenticate`, `fastify.appContext`
  (`request.allowedLocalIds`, `request.user.id`), `fastify.can('arqueo', ...)`.
- Produces: `POST /api/arqueo` (crea un arqueo, calcula `total`/`ingresos`/`gastos`/`comprobacion`),
  `GET /api/arqueo?id_local=X` (lista ordenada `fecha desc`), `GET /api/arqueo/:id` (detalle con
  `detalles` incluidos) — no hay tareas posteriores en este plan que dependan de esto (el frontend es
  otra ronda), pero deben quedar documentadas para esa ronda futura.

- [ ] **Step 1: Crear el archivo de rutas**

Crear `dcsmart/backend/src/routes/arqueo.js`:

```js
// Arqueo: conteo físico de efectivo por local (caja fuerte + cofre + adición),
// comparado contra lo acumulado en el sistema desde el arqueo anterior del
// mismo local. Ver docs/superpowers/specs/2026-07-16-arqueo-design.md para
// la fórmula completa.

// Busca el arqueo anterior de un local (el más reciente con fecha < la nueva).
// Devuelve null si es el primer arqueo del local.
async function getArqueoAnterior(fastify, id_local, fecha) {
  return fastify.db.arqueo.findFirst({
    where: { id_local, fecha: { lt: fecha } },
    orderBy: { fecha: 'desc' }
  })
}

// Suma Caja.efectivo del local en (fechaDesde, fechaHasta] -- fechaDesde exclusivo, fechaHasta inclusivo.
async function calcularIngresos(fastify, id_local, fechaDesde, fechaHasta) {
  const cajas = await fastify.db.caja.findMany({
    where: {
      id_local,
      fecha_inicio: {
        ...(fechaDesde ? { gt: fechaDesde } : {}),
        lte: fechaHasta
      }
    },
    select: { efectivo: true }
  })
  return cajas.reduce((acc, c) => acc + Number(c.efectivo ?? 0), 0)
}

// Suma Pago.importe del local, pagado=true, en efectivo, egreso real, en (fechaDesde, fechaHasta].
async function calcularGastos(fastify, id_local, fechaDesde, fechaHasta) {
  const metodoEfectivo = await fastify.db.metodoPago.findUnique({ where: { nombre: 'Efectivo' } })
  if (!metodoEfectivo) return 0
  const pagos = await fastify.db.pago.findMany({
    where: {
      id_local,
      pagado: true,
      ingresa_egreso: false,
      id_metodo: metodoEfectivo.id,
      fecha_pago: {
        ...(fechaDesde ? { gt: fechaDesde } : {}),
        lte: fechaHasta
      }
    },
    select: { importe: true }
  })
  return pagos.reduce((acc, p) => acc + Number(p.importe ?? 0), 0)
}

export default async function arqueoRoutes(fastify) {
  const viewHandler   = [fastify.authenticate, fastify.appContext, fastify.can('arqueo', 'view')]
  const createHandler = [fastify.authenticate, fastify.appContext, fastify.can('arqueo', 'create')]

  // ── GET / ─────────────────────────────────────────────────────────────
  fastify.get('/', { preHandler: viewHandler }, async (request, reply) => {
    const { id_local } = request.query
    if (!id_local || !request.allowedLocalIds.includes(id_local)) {
      return reply.code(403).send({ error: 'Sin acceso a ese local' })
    }
    const arqueos = await fastify.db.arqueo.findMany({
      where: { id_local },
      orderBy: { fecha: 'desc' }
    })
    return { data: arqueos }
  })

  // ── GET /:id ──────────────────────────────────────────────────────────
  fastify.get('/:id', { preHandler: viewHandler }, async (request, reply) => {
    const arqueo = await fastify.db.arqueo.findUnique({
      where: { id: request.params.id },
      include: { detalles: { include: { detalle_tipo: true } } }
    })
    if (!arqueo) return reply.code(404).send({ error: 'Arqueo no encontrado' })
    if (!request.allowedLocalIds.includes(arqueo.id_local)) {
      return reply.code(403).send({ error: 'Sin acceso' })
    }
    return arqueo
  })

  // ── POST / ────────────────────────────────────────────────────────────
  // body: { id_local, fecha, caja_fuerte, cofre, adicion, detalles?: [{id_tipo?, nombre?, monto}] }
  fastify.post('/', { preHandler: createHandler }, async (request, reply) => {
    const { id_local, fecha, caja_fuerte, cofre, adicion, detalles } = request.body
    if (!id_local || !fecha || caja_fuerte == null || cofre == null || adicion == null) {
      return reply.code(400).send({ error: 'id_local, fecha, caja_fuerte, cofre y adicion son requeridos' })
    }
    if (!request.allowedLocalIds.includes(id_local)) {
      return reply.code(403).send({ error: 'Sin acceso a ese local' })
    }

    const fechaArqueo = new Date(fecha)
    const anterior = await getArqueoAnterior(fastify, id_local, fechaArqueo)
    const totalUltimoArqueo = anterior ? Number(anterior.total) : 0
    const fechaDesde = anterior ? anterior.fecha : null

    const total = Number(caja_fuerte) + Number(cofre) + Number(adicion)
    const ingresos = await calcularIngresos(fastify, id_local, fechaDesde, fechaArqueo)
    const gastos = await calcularGastos(fastify, id_local, fechaDesde, fechaArqueo)
    const comprobacion = (total + totalUltimoArqueo) - (ingresos + gastos)

    const arqueo = await fastify.db.arqueo.create({
      data: {
        id_local,
        fecha: fechaArqueo,
        caja_fuerte: String(caja_fuerte),
        cofre: String(cofre),
        adicion: String(adicion),
        total: String(total),
        ingresos: String(ingresos),
        gastos: String(gastos),
        comprobacion: String(comprobacion),
        created_by: request.user.id,
        detalles: {
          create: (detalles || []).map((d) => ({
            id_tipo: d.id_tipo || null,
            nombre: d.nombre || null,
            monto: String(d.monto)
          }))
        }
      },
      include: { detalles: true }
    })
    return reply.code(201).send(arqueo)
  })
}
```

- [ ] **Step 2: Registrar la ruta en `server.js`**

En `dcsmart/backend/src/server.js`, agregar el import junto a `auditoriasRoutes` (línea 26):

```js
import arqueoRoutes from './routes/arqueo.js'
```

Y el registro junto al de auditorías (línea 84):

```js
await app.register(arqueoRoutes, { prefix: '/api/arqueo' })
```

- [ ] **Step 3: Verificar sintaxis y arranque**

```bash
cd dcsmart/backend
node --check src/routes/arqueo.js
npm run dev
```
Expected: `node --check` sin output, y `Server listening at http://0.0.0.0:3000` sin errores de
import al levantar.

- [ ] **Step 4: Probar el cálculo con datos reales del local de testing**

Con el túnel activo y el backend corriendo, usar el local `testing-local-01` (ya tiene 5 cajas y 8
pagos de ejemplo de una feature anterior) para verificar el cálculo end-to-end sin necesidad de JWT,
llamando directo a las funciones del módulo desde un script Node (no HTTP, evita el tema de
autenticación):

```bash
cd dcsmart/backend
node -e "
import('./src/routes/arqueo.js').then(async () => {}); // solo para confirmar que el módulo importa bien
"
```

Y una verificación funcional real de los cálculos, replicando la lógica de `calcularIngresos`/
`calcularGastos` con una query directa (ya que las funciones no están exportadas individualmente,
esto confirma que los NÚMEROS esperados existen en la base para validar manualmente después vía
Postman/curl con un JWT real cuando el usuario lo pruebe):

```bash
node -e "
import('@prisma/client').then(async ({PrismaClient}) => {
  const prisma = new PrismaClient();
  const cajas = await prisma.caja.findMany({ where: { id_local: 'testing-local-01' }, select: { efectivo: true } });
  const totalEfectivo = cajas.reduce((a,c) => a + Number(c.efectivo ?? 0), 0);
  console.log('suma Caja.efectivo (testing-local-01):', totalEfectivo);
  const metodo = await prisma.metodoPago.findUnique({ where: { nombre: 'Efectivo' } });
  const pagos = await prisma.pago.findMany({ where: { id_local: 'testing-local-01', pagado: true, ingresa_egreso: false, id_metodo: metodo?.id }, select: { importe: true } });
  console.log('suma Pago efectivo pagado/egreso (testing-local-01):', pagos.reduce((a,p)=>a+Number(p.importe??0),0));
  await prisma.\$disconnect();
});
"
```
Expected: ambos comandos corren sin error y muestran números (no necesariamente los mismos del
brief, dependen de los datos reales de `testing-local-01` en ese momento) — sirven para confirmar
que `calcularIngresos`/`calcularGastos` van a operar sobre datos reales existentes cuando se llame al
endpoint real con un `POST /api/arqueo` autenticado.

- [ ] **Step 5: Commit**

```bash
cd dcsmart
git add backend/src/routes/arqueo.js backend/src/server.js
git commit -m "Arqueo: rutas backend (crear con calculo de comprobacion, listar, detalle)"
```

---

### Task 3: Aplicar el módulo de permisos contra la base real

**Files:**
- Create: `dcsmart/backend/scripts/seed-arqueo-permisos.cjs`

**Interfaces:**
- Consumes: módulo `arqueo` y matriz de permisos definida en Task 1 (`seed.js`, como referencia de
  qué valores usar — este script NO importa `seed.js`, solo replica los mismos valores de forma
  aislada y segura).
- Produces: fila `Module` para `'arqueo'` y filas `RolePermission` para los 5 roles existentes
  (`super_admin`, `dcsmart`, `admin`, `cajero`, `reportes`) contra la base real — no expone ninguna
  interfaz para tareas futuras (es la última de este plan; el frontend es otra ronda).

- [ ] **Step 1: Crear el script**

Crear `dcsmart/backend/scripts/seed-arqueo-permisos.cjs`:

```js
// Aplica SOLO el modulo 'arqueo' + sus permisos por rol contra la base real.
// NO toca usuarios, ni ningun otro modulo/rol -- a diferencia de prisma/seed.js
// completo (documentado como destructivo contra datos reales), este script es
// seguro de correr: solo hace upsert de Module y RolePermission para 'arqueo'.
'use strict'
const { PrismaClient } = require('@prisma/client')
const prisma = new PrismaClient()

const T = true, F = false

// Mismos valores que la fila 'arqueo' agregada a MATRIX en prisma/seed.js.
const PERMISOS_ARQUEO = {
  super_admin: [T, T, T, T],
  dcsmart:     [T, T, T, T],
  admin:       [T, T, F, F],
  cajero:      [T, T, F, F],
  reportes:    [F, F, F, F],
}

async function main() {
  const modulo = await prisma.module.upsert({
    where: { nombre: 'arqueo' }, update: {}, create: { nombre: 'arqueo' }
  })
  console.log('Modulo:', modulo.nombre, modulo.id)

  for (const [nombreRol, permisos] of Object.entries(PERMISOS_ARQUEO)) {
    const role = await prisma.role.findUnique({ where: { nombre: nombreRol } })
    if (!role) { console.log(`  rol "${nombreRol}" no existe en la base, se omite`); continue }
    const [can_view, can_create, can_edit, can_delete] = permisos
    const perm = { can_view, can_create, can_edit, can_delete }
    await prisma.rolePermission.upsert({
      where: { id_role_id_module: { id_role: role.id, id_module: modulo.id } },
      update: perm,
      create: { id_role: role.id, id_module: modulo.id, ...perm }
    })
    console.log(`  permisos aplicados para "${nombreRol}":`, permisos)
  }
  console.log('\nListo.')
}

main().catch((e) => { console.error('ERROR:', e); process.exit(1) }).finally(() => prisma.$disconnect())
```

- [ ] **Step 2: Correr el script contra la base real**

Con el túnel `cloud-sql-proxy` activo en el puerto 5433:

```bash
cd dcsmart/backend
node scripts/seed-arqueo-permisos.cjs
```
Expected: `Modulo: arqueo <uuid>`, seguido de 5 líneas `permisos aplicados para "..."` (una por cada
rol existente en la base — si algún rol no existe todavía, se loguea "se omite" en vez de fallar), y
`Listo.` al final.

- [ ] **Step 3: Verificar con una query directa**

```bash
cd dcsmart/backend
node -e "
import('@prisma/client').then(async ({PrismaClient}) => {
  const prisma = new PrismaClient();
  const modulo = await prisma.module.findUnique({ where: { nombre: 'arqueo' } });
  const perms = await prisma.rolePermission.findMany({ where: { id_module: modulo.id }, include: { role: true } });
  console.log(perms.map(p => ({ rol: p.role.nombre, view: p.can_view, create: p.can_create, edit: p.can_edit, delete: p.can_delete })));
  await prisma.\$disconnect();
});
"
```
Expected: un array con una entrada por rol existente, reflejando los valores de `PERMISOS_ARQUEO`
(ej. `super_admin` con los 4 en `true`, `admin`/`cajero` con `view`/`create` en `true` y
`edit`/`delete` en `false`).

- [ ] **Step 4: No commitear el script a git**

Este script es de un solo uso contra la base real (mismo criterio que `seed-testing-group.cjs` de una
feature anterior) — queda en disco sin versionar. No correr `git add` sobre este archivo.

---

## Self-Review

**Cobertura del spec:**
- Modelo `Arqueo` con los 12 campos exactos de la spec → Task 1. ✓
- Modelo `ArqueoDetalle` reusando `DetalleTipo` (sin catálogo nuevo) → Task 1. ✓
- Fórmula de comprobación exacta (`(total + total_ultimo_arqueo) - (ingresos + gastos)`), con
  `total_ultimo_arqueo = 0` si es el primer arqueo del local → Task 2 (`getArqueoAnterior` devuelve
  `null`, `totalUltimoArqueo` cae a `0`). ✓
- `ingresos` = suma `Caja.efectivo` del local, `fecha_inicio` entre arqueo anterior (exclusivo) y
  este (inclusivo) → Task 2 (`calcularIngresos`, filtro `gt`/`lte`). ✓
- `gastos` = suma `Pago.importe` del local, `pagado=true`, `id_metodo=Efectivo`,
  `ingresa_egreso=false`, `fecha_pago` en el mismo rango → Task 2 (`calcularGastos`). ✓
- Snapshot (no se recalcula después) → Task 2 (los valores se calculan una sola vez al `create`, no
  hay ningún endpoint de recálculo). ✓
- Módulo de permisos nuevo `arqueo`, no reusar `caja` → Task 1 (seed.js) + Task 3 (aplicado contra la
  base real). ✓
- Por local (secuencia independiente) → Task 2 (`getArqueoAnterior` siempre filtra por `id_local`). ✓

**Placeholder scan:** sin TBD/TODO. Todo el código de cada step está completo.

**Consistencia de tipos:** `Arqueo.total/ingresos/gastos/comprobacion` son `Decimal` en el schema
(Task 1) y se pasan como `String(numero)` al `create` de Prisma en Task 2 (consistente con el patrón
ya usado en migraciones previas de este proyecto para campos `Decimal`). `getArqueoAnterior` devuelve
un objeto `Arqueo` completo o `null`; en `POST /`, `anterior ? Number(anterior.total) : 0` y
`anterior ? anterior.fecha : null` manejan ambos casos explícitamente, sin asumir que `anterior`
existe. `calcularIngresos`/`calcularGastos` reciben `fechaDesde` que puede ser `null` (primer arqueo)
— el filtro Prisma usa `...(fechaDesde ? { gt: fechaDesde } : {})`, que omite la condición `gt`
completamente cuando es `null` (trae todas las cajas/pagos hasta la fecha del arqueo actual, sin
límite inferior) — comportamiento correcto y consistente en ambas funciones.
