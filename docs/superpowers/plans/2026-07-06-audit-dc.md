# Auditoría DC (doble validación) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar un segundo circuito de auditoría ("audit dc"), exclusivo para los roles `super_admin`/`dcsmart`, que al togglearse cascadea el estado al auditado normal de Pagos y Cajas, sin que `admin`/`cajero` vean ningún rastro de su existencia.

**Architecture:** La tabla genérica `audits` (histórico append-only, ya usada para el auditado normal de pagos y cajas) gana una columna booleana `audit_dc` que distingue el circuito. El backend agrega un endpoint `PATCH /:id/audit-dc` (protegido por un nuevo decorator de rol) en `pagos.js` y `caja.js`, que además de escribir su propia fila cascadea una fila nueva en el circuito normal cuando hace falta. El frontend agrega un botón y un estado más en los mismos paneles de detalle que ya muestran "Auditado".

**Tech Stack:** Fastify + Prisma (Postgres/Cloud SQL) en el backend, React + Zustand + Axios en el frontend. Sin librerías nuevas.

## Global Constraints

- La migración de schema es aditiva (`audit_dc Boolean @default(false)`) contra la base de Cloud SQL compartida por `dev` y `master` — **requiere confirmación explícita del usuario antes de correr `prisma db push`** (ver Tarea 1).
- `admin` y `cajero` no deben ver ningún indicio del circuito DC: ni botón, ni fila de detalle, ni entradas en el historial de auditoría.
- No se bloquea el auditado normal después de que DC audite — sigue editable libremente por `admin`/`cajero` (confirmado en el diseño).
- No hay infraestructura de tests automatizados en este repo (`backend/src/test` no existe, no hay test runner configurado en el frontend). Cada tarea se verifica manualmente con `curl` (backend) o con `npx vite build` + inspección visual (frontend) en lugar de un ciclo TDD formal — es la convención ya usada en el resto del proyecto.
- Todos los comandos de backend se corren desde `backend/`, los de frontend desde `frontend/` (rutas relativas a la raíz del repo `dcsmart/`).

---

## Task 1: Migración de schema — columna `audit_dc`

**Files:**
- Modify: `backend/prisma/schema.prisma` (modelo `Audit`, líneas 440-459)

**Interfaces:**
- Produces: campo `audit_dc: Boolean` (default `false`) en el modelo Prisma `Audit`, disponible como `fastify.db.audit.*` en todas las rutas backend.

- [ ] **Step 1: Agregar la columna al modelo `Audit`**

En `backend/prisma/schema.prisma`, modificar:

```prisma
model Audit {
  id            String    @id @default(uuid()) @db.Uuid
  id_registro   String
  tabla         String
  tipo          String
  accion        String    @default("auditado")
  aprobado      Boolean?  @default(false)
  vigente       Boolean   @default(true)
  id_user       String?
  fecha         DateTime?
  observaciones String?
  created_at    DateTime? @default(now())
  updated_at    DateTime? @default(now())

  user User? @relation("AuditUser", fields: [id_user], references: [id])

  @@index([tabla, id_registro, vigente])
  @@index([tabla, id_registro, fecha])
  @@map("audits")
}
```

por:

```prisma
model Audit {
  id            String    @id @default(uuid()) @db.Uuid
  id_registro   String
  tabla         String
  tipo          String
  accion        String    @default("auditado")
  aprobado      Boolean?  @default(false)
  vigente       Boolean   @default(true)
  audit_dc      Boolean   @default(false)
  id_user       String?
  fecha         DateTime?
  observaciones String?
  created_at    DateTime? @default(now())
  updated_at    DateTime? @default(now())

  user User? @relation("AuditUser", fields: [id_user], references: [id])

  @@index([tabla, id_registro, vigente])
  @@index([tabla, id_registro, fecha])
  @@map("audits")
}
```

- [ ] **Step 2: Generar el cliente de Prisma**

Run (desde `backend/`): `npx prisma generate`
Expected: `✔ Generated Prisma Client` sin errores.

- [ ] **Step 3: Aplicar el cambio a la base real — PAUSA OBLIGATORIA**

Este paso escribe en la base de Cloud SQL compartida por `dev` y `master`. **No ejecutar sin que el usuario lo confirme explícitamente en ese momento** (no vale una autorización genérica dada antes). Si el proxy de Cloud SQL no está corriendo, seguir el PASO 13 de `CLAUDE.md` (`./cloud-sql-proxy PROJECT:REGION:INSTANCE --port 5432`) antes de este paso.

Una vez confirmado, run (desde `backend/`): `npx prisma db push`
Expected: algo como `Your database is now in sync with your Prisma schema` y ningún `data loss` reportado (el cambio es aditivo, no debería advertir nada destructivo).

- [ ] **Step 4: Verificar la columna en la base**

Run (desde `backend/`, con el proxy corriendo):
```bash
npx prisma db execute --stdin <<< "SELECT column_name FROM information_schema.columns WHERE table_name = 'audits' AND column_name = 'audit_dc';"
```
Expected: una fila con `audit_dc`.

- [ ] **Step 5: Commit**

```bash
git add backend/prisma/schema.prisma
git commit -m "feat: agrega columna audit_dc a la tabla audits"
```

---

## Task 2: Decorator de permisos `requireDc`

**Files:**
- Modify: `backend/src/plugins/permissions.js`

**Interfaces:**
- Consumes: `request.activeRole` (string, seteado por `fastify.appContext` — valores posibles: `'super_admin' | 'dcsmart' | 'admin' | 'cajero' | 'reportes'`).
- Produces: `fastify.requireDc` — preHandler de Fastify. Debe usarse siempre **después** de `fastify.appContext` en el array de `preHandler` (depende de `request.activeRole`).

- [ ] **Step 1: Agregar el decorator**

En `backend/src/plugins/permissions.js`, agregar dentro de `permissionsPlugin`, después del decorator `requireSuperAdmin` existente (líneas 6-14):

```javascript
  // Guard: solo super_admin o dcsmart pueden auditar por el circuito DC.
  // Requiere que fastify.appContext haya corrido antes (usa request.activeRole).
  fastify.decorate('requireDc', async (request, reply) => {
    if (!['super_admin', 'dcsmart'].includes(request.activeRole)) {
      return reply.code(403).send({ error: 'Solo DCSmart puede realizar esta acción' })
    }
  })
```

- [ ] **Step 2: Verificar que el archivo no tiene errores de sintaxis**

Run (desde `backend/`): `node --check src/plugins/permissions.js`
Expected: sin output (exit code 0).

- [ ] **Step 3: Commit**

```bash
git add backend/src/plugins/permissions.js
git commit -m "feat: agrega decorator requireDc para el circuito de auditoria DC"
```

---

## Task 3: Backend — endpoint `audit-dc` y corrección de scope en Pagos

**Files:**
- Modify: `backend/src/routes/pagos.js`

**Interfaces:**
- Consumes: `fastify.requireDc` (Task 2), columna `audit_dc` en `fastify.db.audit` (Task 1).
- Produces: `PATCH /pagos/:id/audit-dc` → `{ ok: true, audit: boolean, audit_dc: boolean }`. `GET /pagos/:id/audit-history` sigue devolviendo un array de filas `Audit`, pero filtrado según rol.

- [ ] **Step 1: Corregir el scope de las queries del circuito normal**

En `backend/src/routes/pagos.js`, `getAuditedSet` (líneas 11-23), cambiar:

```javascript
    const rows = await fastify.db.audit.findMany({
      where: { tabla: 'pagos', id_registro: { in: pagoIds }, vigente: true, accion: 'auditado' },
      select: { id_registro: true }
    })
```

por:

```javascript
    const rows = await fastify.db.audit.findMany({
      where: { tabla: 'pagos', id_registro: { in: pagoIds }, audit_dc: false, vigente: true, accion: 'auditado' },
      select: { id_registro: true }
    })
```

En `buildAuditFilter` (líneas 28-47), aplicar el mismo cambio a su `findMany` (agregar `audit_dc: false` al `where`, en el mismo lugar relativo).

- [ ] **Step 2: Corregir el scope del toggle normal**

En el handler `PATCH /:id/audit` (líneas 427-470), cambiar:

```javascript
    const nextAccion = await fastify.db.$transaction(async (tx) => {
      const current = await tx.audit.findFirst({
        where: { tabla: 'pagos', id_registro: request.params.id, vigente: true }
      })

      await tx.audit.updateMany({
        where: { tabla: 'pagos', id_registro: request.params.id, vigente: true },
        data: { vigente: false }
      })

      const accion = current?.accion === 'auditado' ? 'desauditado' : 'auditado'

      await tx.audit.create({
        data: {
          id_registro:   request.params.id,
          tabla:         'pagos',
          tipo:          'auditoria_pago',
          accion,
          aprobado:      accion === 'auditado',
          vigente:       true,
          id_user:       request.user.id,
          fecha:         new Date(),
          observaciones: accion === 'desauditado' ? (observaciones || null) : null
        }
      })

      return accion
    })
```

por:

```javascript
    const nextAccion = await fastify.db.$transaction(async (tx) => {
      const current = await tx.audit.findFirst({
        where: { tabla: 'pagos', id_registro: request.params.id, audit_dc: false, vigente: true }
      })

      await tx.audit.updateMany({
        where: { tabla: 'pagos', id_registro: request.params.id, audit_dc: false, vigente: true },
        data: { vigente: false }
      })

      const accion = current?.accion === 'auditado' ? 'desauditado' : 'auditado'

      await tx.audit.create({
        data: {
          id_registro:   request.params.id,
          tabla:         'pagos',
          tipo:          'auditoria_pago',
          accion,
          aprobado:      accion === 'auditado',
          vigente:       true,
          audit_dc:      false,
          id_user:       request.user.id,
          fecha:         new Date(),
          observaciones: accion === 'desauditado' ? (observaciones || null) : null
        }
      })

      return accion
    })
```

- [ ] **Step 3: Agregar el endpoint `PATCH /:id/audit-dc` con cascada**

Insertar inmediatamente después del cierre del handler `PATCH /:id/audit` (después de la línea `})` que cierra ese handler, antes del comentario `// ── GET /:id/audit-history`):

```javascript
  // ── PATCH /:id/audit-dc ───────────────────────────────────────────────
  // Circuito de auditoría exclusivo de super_admin/dcsmart. Cascadea el
  // estado al circuito normal cuando difiere, sin dejar rastro visible
  // para admin/cajero (observaciones null en la fila cascadeada).
  fastify.patch('/:id/audit-dc', { preHandler: [fastify.authenticate, fastify.appContext, fastify.requireDc] }, async (request, reply) => {
    const pago = await fastify.db.pago.findUnique({
      where: { id: request.params.id },
      select: { id_local: true }
    })
    if (!pago) return reply.code(404).send({ error: 'Pago no encontrado' })

    if (!request.allowedLocalIds.includes(pago.id_local)) {
      return reply.code(403).send({ error: 'Sin acceso' })
    }

    const { observaciones } = request.body ?? {}

    const result = await fastify.db.$transaction(async (tx) => {
      const currentDc = await tx.audit.findFirst({
        where: { tabla: 'pagos', id_registro: request.params.id, audit_dc: true, vigente: true }
      })

      await tx.audit.updateMany({
        where: { tabla: 'pagos', id_registro: request.params.id, audit_dc: true, vigente: true },
        data: { vigente: false }
      })

      const accionDc = currentDc?.accion === 'auditado' ? 'desauditado' : 'auditado'

      await tx.audit.create({
        data: {
          id_registro:   request.params.id,
          tabla:         'pagos',
          tipo:          'auditoria_pago',
          accion:        accionDc,
          aprobado:      accionDc === 'auditado',
          vigente:       true,
          audit_dc:      true,
          id_user:       request.user.id,
          fecha:         new Date(),
          observaciones: accionDc === 'desauditado' ? (observaciones || null) : null
        }
      })

      const currentNormal = await tx.audit.findFirst({
        where: { tabla: 'pagos', id_registro: request.params.id, audit_dc: false, vigente: true }
      })

      let accionNormal = currentNormal?.accion === 'auditado' ? 'auditado' : 'desauditado'

      if (accionNormal !== accionDc) {
        await tx.audit.updateMany({
          where: { tabla: 'pagos', id_registro: request.params.id, audit_dc: false, vigente: true },
          data: { vigente: false }
        })

        await tx.audit.create({
          data: {
            id_registro:   request.params.id,
            tabla:         'pagos',
            tipo:          'auditoria_pago',
            accion:        accionDc,
            aprobado:      accionDc === 'auditado',
            vigente:       true,
            audit_dc:      false,
            id_user:       request.user.id,
            fecha:         new Date(),
            observaciones: null
          }
        })

        accionNormal = accionDc
      }

      return { audit_dc: accionDc === 'auditado', audit: accionNormal === 'auditado' }
    })

    return { ok: true, ...result }
  })

```

- [ ] **Step 4: Filtrar el historial por rol**

En `GET /:id/audit-history` (líneas 474-489), cambiar:

```javascript
    return fastify.db.audit.findMany({
      where: { tabla: 'pagos', id_registro: request.params.id },
      orderBy: { fecha: 'desc' },
      include: { user: { select: { id: true, nombre: true } } }
    })
```

por:

```javascript
    const isDc = ['super_admin', 'dcsmart'].includes(request.activeRole)

    return fastify.db.audit.findMany({
      where: {
        tabla: 'pagos',
        id_registro: request.params.id,
        ...(isDc ? {} : { audit_dc: false })
      },
      orderBy: { fecha: 'desc' },
      include: { user: { select: { id: true, nombre: true } } }
    })
```

- [ ] **Step 5: Verificar sintaxis**

Run (desde `backend/`): `node --check src/routes/pagos.js`
Expected: sin output (exit code 0).

- [ ] **Step 6: Verificar manualmente contra la base (requiere backend corriendo y un pago de prueba)**

Con el backend corriendo (`npm run dev` en `backend/`) y un token de un usuario `dcsmart` o `super_admin`:

```bash
curl -X PATCH http://localhost:3000/api/pagos/<ID_DE_UN_PAGO>/audit-dc \
  -H "Authorization: Bearer <TOKEN_DCSMART>" \
  -H "X-App-Id: <ID_APP>" \
  -H "Content-Type: application/json"
```

Expected: `{"ok":true,"audit":true,"audit_dc":true}` la primera vez (si el pago no estaba auditado). Repetir la llamada debe devolver `{"ok":true,"audit":false,"audit_dc":false}`.

Con un token de un usuario `admin` (no `dcsmart`/`super_admin`), la misma llamada debe devolver `403`.

- [ ] **Step 7: Commit**

```bash
git add backend/src/routes/pagos.js
git commit -m "feat: endpoint audit-dc con cascada para pagos, corrige scope del circuito normal"
```

---

## Task 4: Backend — endpoint `audit-dc` y corrección de scope en Cajas

**Files:**
- Modify: `backend/src/routes/caja.js`

**Interfaces:**
- Consumes: igual que Task 3 (`fastify.requireDc`, columna `audit_dc`).
- Produces: `PATCH /cajas/:id/audit-dc` → `{ ok: true, audit: boolean, audit_dc: boolean }`. `GET /cajas/:id/audit-history` filtrado por rol.

Mismo patrón que la Task 3, aplicado a `caja.js` (tabla `'cajas'`, tipo `'auditoria_caja'`, modelo `fastify.db.caja`).

- [ ] **Step 1: Corregir el scope de `getAuditedCajaSet` y `buildCajaAuditFilter`**

En `backend/src/routes/caja.js` (líneas 8-20 y 22-41), agregar `audit_dc: false` a los dos `findMany` que leen el estado vigente (mismo cambio que Task 3 Step 1, adaptado a `tabla: 'cajas'`).

- [ ] **Step 2: Corregir el scope del toggle normal**

En `PATCH /:id/audit` (líneas 299-342), cambiar:

```javascript
    const nextAccion = await fastify.db.$transaction(async (tx) => {
      const current = await tx.audit.findFirst({
        where: { tabla: 'cajas', id_registro: request.params.id, vigente: true }
      })

      await tx.audit.updateMany({
        where: { tabla: 'cajas', id_registro: request.params.id, vigente: true },
        data: { vigente: false }
      })

      const accion = current?.accion === 'auditado' ? 'desauditado' : 'auditado'

      await tx.audit.create({
        data: {
          id_registro:   request.params.id,
          tabla:         'cajas',
          tipo:          'auditoria_caja',
          accion,
          aprobado:      accion === 'auditado',
          vigente:       true,
          id_user:       request.user.id,
          fecha:         new Date(),
          observaciones: accion === 'desauditado' ? (observaciones || null) : null
        }
      })

      return accion
    })
```

por:

```javascript
    const nextAccion = await fastify.db.$transaction(async (tx) => {
      const current = await tx.audit.findFirst({
        where: { tabla: 'cajas', id_registro: request.params.id, audit_dc: false, vigente: true }
      })

      await tx.audit.updateMany({
        where: { tabla: 'cajas', id_registro: request.params.id, audit_dc: false, vigente: true },
        data: { vigente: false }
      })

      const accion = current?.accion === 'auditado' ? 'desauditado' : 'auditado'

      await tx.audit.create({
        data: {
          id_registro:   request.params.id,
          tabla:         'cajas',
          tipo:          'auditoria_caja',
          accion,
          aprobado:      accion === 'auditado',
          vigente:       true,
          audit_dc:      false,
          id_user:       request.user.id,
          fecha:         new Date(),
          observaciones: accion === 'desauditado' ? (observaciones || null) : null
        }
      })

      return accion
    })
```

- [ ] **Step 3: Agregar el endpoint `PATCH /:id/audit-dc`**

Insertar después del cierre del handler `PATCH /:id/audit`, antes del comentario `// ── GET /:id/audit-history`:

```javascript
  // ── PATCH /:id/audit-dc ───────────────────────────────────────────────
  // Ver pagos.js para la explicación completa del mecanismo de cascada.
  fastify.patch('/:id/audit-dc', { preHandler: [fastify.authenticate, fastify.appContext, fastify.requireDc] }, async (request, reply) => {
    const caja = await fastify.db.caja.findUnique({
      where: { id: request.params.id },
      select: { id_local: true }
    })
    if (!caja) return reply.code(404).send({ error: 'Caja no encontrada' })

    if (!request.allowedLocalIds.includes(caja.id_local)) {
      return reply.code(403).send({ error: 'Sin acceso' })
    }

    const { observaciones } = request.body ?? {}

    const result = await fastify.db.$transaction(async (tx) => {
      const currentDc = await tx.audit.findFirst({
        where: { tabla: 'cajas', id_registro: request.params.id, audit_dc: true, vigente: true }
      })

      await tx.audit.updateMany({
        where: { tabla: 'cajas', id_registro: request.params.id, audit_dc: true, vigente: true },
        data: { vigente: false }
      })

      const accionDc = currentDc?.accion === 'auditado' ? 'desauditado' : 'auditado'

      await tx.audit.create({
        data: {
          id_registro:   request.params.id,
          tabla:         'cajas',
          tipo:          'auditoria_caja',
          accion:        accionDc,
          aprobado:      accionDc === 'auditado',
          vigente:       true,
          audit_dc:      true,
          id_user:       request.user.id,
          fecha:         new Date(),
          observaciones: accionDc === 'desauditado' ? (observaciones || null) : null
        }
      })

      const currentNormal = await tx.audit.findFirst({
        where: { tabla: 'cajas', id_registro: request.params.id, audit_dc: false, vigente: true }
      })

      let accionNormal = currentNormal?.accion === 'auditado' ? 'auditado' : 'desauditado'

      if (accionNormal !== accionDc) {
        await tx.audit.updateMany({
          where: { tabla: 'cajas', id_registro: request.params.id, audit_dc: false, vigente: true },
          data: { vigente: false }
        })

        await tx.audit.create({
          data: {
            id_registro:   request.params.id,
            tabla:         'cajas',
            tipo:          'auditoria_caja',
            accion:        accionDc,
            aprobado:      accionDc === 'auditado',
            vigente:       true,
            audit_dc:      false,
            id_user:       request.user.id,
            fecha:         new Date(),
            observaciones: null
          }
        })

        accionNormal = accionDc
      }

      return { audit_dc: accionDc === 'auditado', audit: accionNormal === 'auditado' }
    })

    return { ok: true, ...result }
  })

```

- [ ] **Step 4: Filtrar el historial por rol**

En `GET /:id/audit-history` (líneas 345-360), aplicar el mismo cambio que Task 3 Step 4, adaptado a `tabla: 'cajas'`.

- [ ] **Step 5: Verificar sintaxis**

Run (desde `backend/`): `node --check src/routes/caja.js`
Expected: sin output (exit code 0).

- [ ] **Step 6: Verificar manualmente**

Mismo patrón que Task 3 Step 6, contra `PATCH /api/cajas/<ID_DE_UNA_CAJA>/audit-dc`.

- [ ] **Step 7: Commit**

```bash
git add backend/src/routes/caja.js
git commit -m "feat: endpoint audit-dc con cascada para cajas, corrige scope del circuito normal"
```

---

## Task 5: Frontend — clientes API

**Files:**
- Modify: `frontend/src/api/pagos.js`
- Modify: `frontend/src/api/cajas.js`

**Interfaces:**
- Produces: `pagosApi.auditDc(id, data)`, `cajasApi.auditDc(id, data)` — ambos `PATCH .../audit-dc`, mismo shape de `data` que `audit()` (`{ observaciones }` opcional), devuelven `{ data: { ok, audit, audit_dc } }` (axios envuelve en `.data`).

- [ ] **Step 1: Agregar `auditDc` a `pagosApi`**

En `frontend/src/api/pagos.js`, después de la línea `auditHistory: (id) => client.get(...)`, agregar:

```javascript
  auditDc:      (id, data)     => client.patch(`/pagos/${id}/audit-dc`,     data),
```

- [ ] **Step 2: Agregar `auditDc` a `cajasApi`**

En `frontend/src/api/cajas.js`, después de la línea `auditHistory: (id) => client.get(...)`, agregar:

```javascript
  auditDc:      (id, data)       => client.patch(`/cajas/${id}/audit-dc`, data),
```

- [ ] **Step 3: Verificar sintaxis**

Run (desde `frontend/`): `npx eslint src/api/pagos.js src/api/cajas.js`
Expected: sin errores nuevos (puede haber warnings preexistentes no relacionados; no debe haber errores de sintaxis en las líneas agregadas).

- [ ] **Step 4: Commit**

```bash
git add frontend/src/api/pagos.js frontend/src/api/cajas.js
git commit -m "feat: agrega metodo auditDc a los clientes API de pagos y cajas"
```

---

## Task 6: Frontend — botón y estado "Audit DC" en Pagos

**Files:**
- Modify: `frontend/src/pages/pagos/PagoList.jsx`

**Interfaces:**
- Consumes: `pagosApi.auditDc` (Task 5).
- Produces: prop `canAuditDc` en `PagoDetailPanel`; nueva fila visible "Audit DC" y botón homónimo, solo si `canAuditDc`.

- [ ] **Step 1: Aceptar la prop `canAuditDc` en `PagoDetailPanel`**

Cambiar (línea 140):

```javascript
function PagoDetailPanel({ pago, navigate, onDelete, onAudit, onPatch, metodos = [], canEdit = false, canDelete = false }) {
```

por:

```javascript
function PagoDetailPanel({ pago, navigate, onDelete, onAudit, onPatch, metodos = [], canEdit = false, canDelete = false, canAuditDc = false }) {
```

- [ ] **Step 2: Agregar el estado `auditedDc`**

Cambiar (línea 151):

```javascript
  const [audited,      setAudited]      = useState(pago.audit)
  const [auditando,    setAuditando]    = useState(false)
```

por:

```javascript
  const [audited,      setAudited]      = useState(pago.audit)
  const [auditando,    setAuditando]    = useState(false)
  const [auditedDc,    setAuditedDc]    = useState(pago.audit_dc)
  const [auditandoDc,  setAuditandoDc]  = useState(false)
```

- [ ] **Step 3: Agregar el handler `handlePanelAuditDc`**

Después de `handlePanelAudit` (después de la línea 290, que cierra esa función con `}`), agregar:

```javascript

  const handlePanelAuditDc = async () => {
    let observaciones
    if (auditedDc) {
      observaciones = await showPrompt(
        'Esta orden ya tiene audit DC. ¿Querés revertirlo? Podés dejar un motivo.',
        { placeholder: 'Motivo (opcional)' }
      )
      if (observaciones === null) return
    }
    setAuditandoDc(true)
    try {
      const { data } = await pagosApi.auditDc(pago.id, auditedDc ? { observaciones } : undefined)
      setAuditedDc(data.audit_dc)
      setAudited(data.audit)
      notify(data.audit_dc ? 'Audit DC aplicado' : 'Audit DC revertido', 'success')
      onAudit?.(pago.id, data.audit)
      loadAuditHistory()
    } catch { notify('Error al auditar (DC)', 'error') }
    finally { setAuditandoDc(false) }
  }
```

- [ ] **Step 4: Agregar el botón "Audit DC" al menú de acciones**

Cambiar (líneas 384-394):

```javascript
          {canEdit && (
            <button
              className={`btn ${audited ? 'btn-secondary' : 'btn-primary'}`}
              onClick={handlePanelAudit}
              disabled={auditando}
            >
              {auditando
                ? <span className="spinner" style={{ width: 14, height: 14, borderWidth: 2 }} />
                : audited ? '✓ Auditado' : 'Auditar'
              }
            </button>
          )}
```

por:

```javascript
          {canEdit && (
            <button
              className={`btn ${audited ? 'btn-secondary' : 'btn-primary'}`}
              onClick={handlePanelAudit}
              disabled={auditando}
            >
              {auditando
                ? <span className="spinner" style={{ width: 14, height: 14, borderWidth: 2 }} />
                : audited ? '✓ Auditado' : 'Auditar'
              }
            </button>
          )}
          {canAuditDc && (
            <button
              className={`btn ${auditedDc ? 'btn-secondary' : 'btn-primary'}`}
              onClick={handlePanelAuditDc}
              disabled={auditandoDc}
            >
              {auditandoDc
                ? <span className="spinner" style={{ width: 14, height: 14, borderWidth: 2 }} />
                : auditedDc ? '✓ Audit DC' : 'Audit DC'
              }
            </button>
          )}
```

- [ ] **Step 5: Agregar la fila "Audit DC" a `infoRows`, condicional a `canAuditDc`**

Cambiar (líneas 352-373, el cierre del array `infoRows`):

```javascript
    ['Auditado',    audited ? 'Sí' : 'No'],
    ['Periódico',   periodico ? 'Sí' : 'No'],
  ]
```

por:

```javascript
    ['Auditado',    audited ? 'Sí' : 'No'],
    ...(canAuditDc ? [['Audit DC', auditedDc ? 'Sí' : 'No']] : []),
    ['Periódico',   periodico ? 'Sí' : 'No'],
  ]
```

- [ ] **Step 6: Calcular `canAuditDc` en el componente padre y pasarlo**

Cambiar (líneas 703-705):

```javascript
  const role        = activeApp?.role
  const canEdit     = ['super_admin', 'dcsmart', 'admin'].includes(role)
  const canDelete   = ['super_admin', 'dcsmart'].includes(role)
```

por:

```javascript
  const role        = activeApp?.role
  const canEdit     = ['super_admin', 'dcsmart', 'admin'].includes(role)
  const canDelete   = ['super_admin', 'dcsmart'].includes(role)
  const canAuditDc  = ['super_admin', 'dcsmart'].includes(role)
```

Y cambiar (línea 1386):

```javascript
          <PagoDetailPanel pago={selectedPago} navigate={navigate} onDelete={handleDelete} onAudit={patchPagoAudit} onPatch={patchPago} metodos={metodos} canEdit={canEdit} canDelete={canDelete} />
```

por:

```javascript
          <PagoDetailPanel pago={selectedPago} navigate={navigate} onDelete={handleDelete} onAudit={patchPagoAudit} onPatch={patchPago} metodos={metodos} canEdit={canEdit} canDelete={canDelete} canAuditDc={canAuditDc} />
```

- [ ] **Step 7: Build de verificación**

Run (desde `frontend/`): `npx vite build`
Expected: `✓ built in <tiempo>` sin errores.

- [ ] **Step 8: Verificación manual en el navegador**

Con el frontend corriendo (`npm run dev` en `frontend/`), loguearse como usuario `dcsmart` o `super_admin`, abrir un pago desde `/pagos`, y confirmar: aparece el botón "Audit DC" y la fila "Audit DC: No/Sí" en el detalle; al hacer click, el botón "Auditar" normal también cambia de estado. Repetir logueado como `admin`: el botón y la fila "Audit DC" no deben aparecer en absoluto.

- [ ] **Step 9: Commit**

```bash
git add frontend/src/pages/pagos/PagoList.jsx
git commit -m "feat: boton y estado Audit DC en el detalle de pagos"
```

---

## Task 7: Frontend — botón y estado "Audit DC" en Cajas

**Files:**
- Modify: `frontend/src/pages/cajas/CajaDetail.jsx`

**Interfaces:**
- Consumes: `cajasApi.auditDc` (Task 5).
- Produces: mismo patrón visual que Task 6, aplicado a la página de detalle de caja (no hay panel/drawer separado acá, todo vive en `CajaDetail.jsx`).

- [ ] **Step 1: Importar `useAppStore` y calcular `canAuditDc`**

`CajaDetail.jsx` hoy NO importa `useAppStore` (solo importa `useUiStore`). Agregar el import, en la línea 5, junto a los demás imports:

```javascript
import { useAppStore } from '../../store/appStore.js'
```

Dentro de `export default function CajaDetail()`, junto a las demás declaraciones de estado del componente (después de `const showPrompt  = useUiStore((s) => s.showPrompt)`, línea 45), agregar:

```javascript
  const role       = useAppStore((s) => s.activeApp)?.role
  const canAuditDc = ['super_admin', 'dcsmart'].includes(role)
  const [auditandoDc, setAuditandoDc] = useState(false)
```

- [ ] **Step 2: Agregar el handler `handleAuditDc`**

Después de `handleAudit` (después del `finally { setAuditando(false) }` que cierra esa función, línea 110-111), agregar:

```javascript

  const handleAuditDc = async () => {
    let observaciones
    if (caja.audit_dc) {
      observaciones = await showPrompt(
        'Esta caja ya tiene audit DC. ¿Querés revertirlo? Podés dejar un motivo.',
        { placeholder: 'Motivo (opcional)' }
      )
      if (observaciones === null) return
    }
    setAuditandoDc(true)
    try {
      const { data } = await cajasApi.auditDc(id, caja.audit_dc ? { observaciones } : undefined)
      notify(data.audit_dc ? 'Audit DC aplicado' : 'Audit DC revertido', 'success')
      setCaja(prev => ({ ...prev, audit_dc: data.audit_dc, audit: data.audit }))
      loadAuditHistory()
    } catch { notify('Error al auditar (DC)', 'error') }
    finally { setAuditandoDc(false) }
  }
```

- [ ] **Step 3: Agregar el botón "Audit DC" junto al botón "Auditar"**

Cambiar (líneas 145-156):

```javascript
        <div className="page-actions">
          <button
            className={`btn ${caja.audit ? 'btn-secondary' : 'btn-primary'}`}
            onClick={handleAudit}
            disabled={auditando}
          >
            {auditando
              ? <span className="spinner" style={{ width: 14, height: 14, borderWidth: 2 }} />
              : caja.audit ? '✓ Auditado' : 'Auditar'
            }
          </button>
        </div>
```

por:

```javascript
        <div className="page-actions">
          <button
            className={`btn ${caja.audit ? 'btn-secondary' : 'btn-primary'}`}
            onClick={handleAudit}
            disabled={auditando}
          >
            {auditando
              ? <span className="spinner" style={{ width: 14, height: 14, borderWidth: 2 }} />
              : caja.audit ? '✓ Auditado' : 'Auditar'
            }
          </button>
          {canAuditDc && (
            <button
              className={`btn ${caja.audit_dc ? 'btn-secondary' : 'btn-primary'}`}
              onClick={handleAuditDc}
              disabled={auditandoDc}
              style={{ marginLeft: '0.5rem' }}
            >
              {auditandoDc
                ? <span className="spinner" style={{ width: 14, height: 14, borderWidth: 2 }} />
                : caja.audit_dc ? '✓ Audit DC' : 'Audit DC'
              }
            </button>
          )}
        </div>
```

- [ ] **Step 4: Agregar la fila "Audit DC" a `infoRows`, condicional a `canAuditDc`**

Cambiar (líneas 118-130):

```javascript
  const infoRows = [
    ['Local',        caja.local?.nombre ?? '—'],
    ['Inicio',       fmtDT(caja.fecha_inicio)],
    ['Cierre',       fmtDT(caja.fecha_cierre)],
    ['Cajero',       caja.cajero ?? '—'],
    ['Total',        fmt$(caja.total),   true],
    ['Efectivo',     fmt$(caja.efectivo)],
    ['Fiscal',       fmt$(caja.fiscal)],
    ['Comensales',   caja.comensales ?? '—'],
    ['Tickets',      caja.tickets ?? '—'],
    ['Origen',       caja.origin ?? '—'],
    ['Auditado',     caja.audit ? 'Sí' : 'No'],
  ]
```

por:

```javascript
  const infoRows = [
    ['Local',        caja.local?.nombre ?? '—'],
    ['Inicio',       fmtDT(caja.fecha_inicio)],
    ['Cierre',       fmtDT(caja.fecha_cierre)],
    ['Cajero',       caja.cajero ?? '—'],
    ['Total',        fmt$(caja.total),   true],
    ['Efectivo',     fmt$(caja.efectivo)],
    ['Fiscal',       fmt$(caja.fiscal)],
    ['Comensales',   caja.comensales ?? '—'],
    ['Tickets',      caja.tickets ?? '—'],
    ['Origen',       caja.origin ?? '—'],
    ['Auditado',     caja.audit ? 'Sí' : 'No'],
    ...(canAuditDc ? [['Audit DC', caja.audit_dc ? 'Sí' : 'No']] : []),
  ]
```

- [ ] **Step 5: Verificar que `cajasApi` esté importado**

Confirmar que `frontend/src/pages/cajas/CajaDetail.jsx` ya importa `cajasApi` desde `'../../api/cajas.js'` (ya lo usa `handleAudit`); si por algún motivo no estuviera, agregar el import junto a los demás.

- [ ] **Step 6: Build de verificación**

Run (desde `frontend/`): `npx vite build`
Expected: `✓ built in <tiempo>` sin errores.

- [ ] **Step 7: Verificación manual en el navegador**

Con el frontend corriendo, loguearse como `dcsmart`/`super_admin`, abrir `/cajas/:id` de una caja de prueba, confirmar que aparece el botón "Audit DC" y la fila correspondiente, y que togglearlo también cambia el botón "Auditar" normal. Repetir como `admin`: no debe aparecer nada de esto.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/pages/cajas/CajaDetail.jsx
git commit -m "feat: boton y estado Audit DC en el detalle de cajas"
```
